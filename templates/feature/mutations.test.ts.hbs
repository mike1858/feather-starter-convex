// @generated-start imports
import { describe, expect } from "vitest";
import { api } from "../_generated/api";
import { test } from "../test.setup";
// @generated-end imports

// @custom-start imports
// @custom-end imports

// @generated-start test-create
describe("create", () => {
  test("creates a {{label}} with default values", async ({
    client,
    userId,
    testClient,
  }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Test {{@key}}",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Test {{@key}} content",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records).toHaveLength(1);
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
    expect(records[0].{{@key}}).toBe("Test {{@key}}");
{{/ifEq}}
{{#ifEq this.type "text"}}
    expect(records[0].{{@key}}).toBe("Test {{@key}} content");
{{/ifEq}}
{{#ifEq this.type "number"}}
    expect(records[0].{{@key}}).toBe(1);
{{/ifEq}}
{{#ifEq this.type "boolean"}}
    expect(records[0].{{@key}}).toBe(true);
{{/ifEq}}
{{/if}}
{{#ifEq this.type "enum"}}
{{#if this.transitions}}
    expect(records[0].{{@key}}).toBe("{{this.values.[0]}}");
{{/if}}
{{/ifEq}}
{{#ifEq this.type "boolean"}}
{{#unless this.required}}
    expect(records[0].{{@key}}).toBe({{#if this.default}}{{this.default}}{{else}}false{{/if}});
{{/unless}}
{{/ifEq}}
{{/each}}
    expect(records[0].creatorId).toBe(userId);
{{#if behaviors.assignable}}
    expect(records[0].assigneeId).toBe(userId);
{{/if}}
{{#if behaviors.orderable}}
    expect(records[0].position).toBeTypeOf("number");
{{/if}}
  });

  test("creates a {{label}} with all fields specified", async ({
    client,
    testClient,
  }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#ifEq this.type "string"}}
      {{@key}}: "Full {{@key}}",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "A detailed {{@key}}",
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 42,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records).toHaveLength(1);
{{#each fields}}
{{#ifEq this.type "string"}}
    expect(records[0].{{@key}}).toBe("Full {{@key}}");
{{/ifEq}}
{{#ifEq this.type "text"}}
    expect(records[0].{{@key}}).toBe("A detailed {{@key}}");
{{/ifEq}}
{{#ifEq this.type "boolean"}}
    expect(records[0].{{@key}}).toBe(true);
{{/ifEq}}
{{#ifEq this.type "number"}}
    expect(records[0].{{@key}}).toBe(42);
{{/ifEq}}
{{/each}}
  });

  test("does nothing when unauthenticated", async ({ testClient }) => {
    await testClient.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Should not be created",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Should not be created",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records).toHaveLength(0);
  });
});
// @generated-end test-create

// @generated-start test-update
describe("update", () => {
  test("does nothing when unauthenticated", async ({ testClient }) => {
    const recordId = await testClient.run(async (ctx: any) => {
      const userId = await ctx.db.insert("users", { name: "Seed" });
      return ctx.db.insert("{{name}}", {
{{#each fields}}
{{#ifEq this.type "string"}}
        {{@key}}: "Seed",
{{/ifEq}}
{{#ifEq this.type "text"}}
        {{@key}}: "Seed content",
{{/ifEq}}
{{#ifEq this.type "boolean"}}
        {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "number"}}
        {{@key}}: 0,
{{/ifEq}}
{{#ifEq this.type "enum"}}
        {{@key}}: "{{this.values.[0]}}",
{{/ifEq}}
{{/each}}
        creatorId: userId,
{{#if behaviors.assignable}}
        assigneeId: userId,
{{/if}}
{{#if behaviors.orderable}}
        position: 1,
{{/if}}
      });
    });

    await testClient.mutation(api.{{name}}.mutations.update, {
      {{camelCase name}}Id: recordId,
{{#each fields}}
{{#ifEq this.type "string"}}
{{#if @first}}
      {{@key}}: "Should not change",
{{/if}}
{{/ifEq}}
{{/each}}
    });

    const record = await testClient.run(async (ctx: any) => ctx.db.get(recordId));
{{#each fields}}
{{#ifEq this.type "string"}}
{{#if @first}}
    expect(record.{{@key}}).toBe("Seed");
{{/if}}
{{/ifEq}}
{{/each}}
  });

  test("updates only specified fields", async ({
    client,
    testClient,
  }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Original {{@key}}",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Original {{@key}}",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    const recordId = records[0]._id;

    await client.mutation(api.{{name}}.mutations.update, {
      {{camelCase name}}Id: recordId,
{{#each fields}}
{{#ifEq this.type "string"}}
{{#if @first}}
      {{@key}}: "Updated {{@key}}",
{{/if}}
{{/ifEq}}
{{/each}}
    });

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(recordId),
    );
{{#each fields}}
{{#ifEq this.type "string"}}
{{#if @first}}
    expect(updated.{{@key}}).toBe("Updated {{@key}}");
{{/if}}
{{/ifEq}}
{{/each}}
  });

  test("throws when {{label}} not found", async ({ client, testClient }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Temporary",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Temporary",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });
    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    const recordId = records[0]._id;
    await testClient.run(async (ctx: any) => ctx.db.delete(recordId));

    await expect(
      client.mutation(api.{{name}}.mutations.update, {
        {{camelCase name}}Id: recordId,
{{#each fields}}
{{#ifEq this.type "string"}}
{{#if @first}}
        {{@key}}: "Nope",
{{/if}}
{{/ifEq}}
{{/each}}
      }),
    ).rejects.toThrow("not found");
  });
});
// @generated-end test-update

// @generated-start test-remove
describe("remove", () => {
  test("does nothing when unauthenticated", async ({ testClient }) => {
    const recordId = await testClient.run(async (ctx: any) => {
      const userId = await ctx.db.insert("users", { name: "Seed" });
      return ctx.db.insert("{{name}}", {
{{#each fields}}
{{#ifEq this.type "string"}}
        {{@key}}: "Seed",
{{/ifEq}}
{{#ifEq this.type "text"}}
        {{@key}}: "Seed content",
{{/ifEq}}
{{#ifEq this.type "boolean"}}
        {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "number"}}
        {{@key}}: 0,
{{/ifEq}}
{{#ifEq this.type "enum"}}
        {{@key}}: "{{this.values.[0]}}",
{{/ifEq}}
{{/each}}
        creatorId: userId,
{{#if behaviors.assignable}}
        assigneeId: userId,
{{/if}}
{{#if behaviors.orderable}}
        position: 1,
{{/if}}
      });
    });

    await testClient.mutation(api.{{name}}.mutations.remove, { {{camelCase name}}Id: recordId });

    const record = await testClient.run(async (ctx: any) => ctx.db.get(recordId));
    expect(record).not.toBeNull();
  });

{{#if behaviors.softDelete}}
  test("soft deletes a {{label}}", async ({ client, testClient }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "To delete",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "To delete",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records).toHaveLength(1);

    await client.mutation(api.{{name}}.mutations.remove, {
      {{camelCase name}}Id: records[0]._id,
    });

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(records[0]._id),
    );
    expect(updated).not.toBeNull();
    expect(updated.deletedAt).toBeTypeOf("number");
  });
{{else}}
  test("deletes a {{label}}", async ({ client, testClient }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "To delete",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "To delete",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records).toHaveLength(1);

    await client.mutation(api.{{name}}.mutations.remove, {
      {{camelCase name}}Id: records[0]._id,
    });

    const remaining = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(remaining).toHaveLength(0);
  });
{{/if}}
});
// @generated-end test-remove

{{#each fields}}
{{#ifEq this.type "enum"}}
{{#if this.transitions}}
// @generated-start test-updateStatus
describe("update{{pascalCase @key}}", () => {
  test("does nothing when unauthenticated", async ({ testClient }) => {
    const recordId = await testClient.run(async (ctx: any) => {
      const userId = await ctx.db.insert("users", { name: "Seed" });
      return ctx.db.insert("{{../name}}", {
{{#each ../fields}}
{{#ifEq this.type "string"}}
        {{@key}}: "Seed",
{{/ifEq}}
{{#ifEq this.type "text"}}
        {{@key}}: "Seed content",
{{/ifEq}}
{{#ifEq this.type "boolean"}}
        {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "number"}}
        {{@key}}: 0,
{{/ifEq}}
{{#ifEq this.type "enum"}}
        {{@key}}: "{{this.values.[0]}}",
{{/ifEq}}
{{/each}}
        creatorId: userId,
{{#if ../behaviors.assignable}}
        assigneeId: userId,
{{/if}}
{{#if ../behaviors.orderable}}
        position: 1,
{{/if}}
      });
    });

    await testClient.mutation(api.{{../name}}.mutations.update{{pascalCase @key}}, {
      {{camelCase ../name}}Id: recordId,
      {{@key}}: "{{this.values.[1]}}",
    });

    const record = await testClient.run(async (ctx: any) => ctx.db.get(recordId));
    expect(record.{{@key}}).toBe("{{this.values.[0]}}");
  });

  test("throws when {{../label}} not found", async ({ client, testClient }) => {
    await client.mutation(api.{{../name}}.mutations.create, {
{{#each ../fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Temp",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Temp",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });
    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{../name}}").collect(),
    );
    const recordId = records[0]._id;
    await testClient.run(async (ctx: any) => ctx.db.delete(recordId));

    await expect(
      client.mutation(api.{{../name}}.mutations.update{{pascalCase @key}}, {
        {{camelCase ../name}}Id: recordId,
        {{@key}}: "{{this.values.[1]}}",
      }),
    ).rejects.toThrow("not found");
  });

{{#each this.transitions}}
  test("advances {{@key}} to {{#each this}}{{this}}{{#unless @last}} then {{/unless}}{{/each}}", async ({ client, testClient }) => {
    await client.mutation(api.{{../../name}}.mutations.create, {
{{#each ../../fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Status test",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Status test",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{../../name}}").collect(),
    );

{{#each this}}
    await client.mutation(api.{{../../../name}}.mutations.update{{pascalCase ../../statusFieldKey}}, {
      {{camelCase ../../../name}}Id: records[0]._id,
      {{../../statusFieldKey}}: "{{this}}",
    });
{{/each}}

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(records[0]._id),
    );
    expect(updated.{{../statusFieldKey}}).toBe("{{#each this}}{{#if @last}}{{this}}{{/if}}{{/each}}");
  });
{{/each}}

  test("rejects invalid transition", async ({
    client,
    testClient,
  }) => {
    await client.mutation(api.{{../name}}.mutations.create, {
{{#each ../fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Status test",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Status test",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{../name}}").collect(),
    );

    await expect(
      client.mutation(api.{{../name}}.mutations.update{{pascalCase @key}}, {
        {{camelCase ../name}}Id: records[0]._id,
        {{@key}}: "{{this.values.[length-1]}}",
      }),
    ).rejects.toThrow("Invalid status transition");
  });
});
// @generated-end test-updateStatus
{{/if}}
{{/ifEq}}
{{/each}}

{{#if behaviors.assignable}}
// @generated-start test-assign
describe("assign", () => {
  test("does nothing when unauthenticated", async ({ testClient }) => {
    const recordId = await testClient.run(async (ctx: any) => {
      const userId = await ctx.db.insert("users", { name: "Seed" });
      return ctx.db.insert("{{name}}", {
{{#each fields}}
{{#ifEq this.type "string"}}
        {{@key}}: "Seed",
{{/ifEq}}
{{#ifEq this.type "text"}}
        {{@key}}: "Seed content",
{{/ifEq}}
{{#ifEq this.type "boolean"}}
        {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "number"}}
        {{@key}}: 0,
{{/ifEq}}
{{#ifEq this.type "enum"}}
        {{@key}}: "{{this.values.[0]}}",
{{/ifEq}}
{{/each}}
        creatorId: userId,
        assigneeId: userId,
{{#if behaviors.orderable}}
        position: 1,
{{/if}}
      });
    });

    const otherUserId = await testClient.run(async (ctx: any) =>
      ctx.db.insert("users", { name: "Other" }),
    );

    await testClient.mutation(api.{{name}}.mutations.assign, {
      {{camelCase name}}Id: recordId,
      assigneeId: otherUserId,
    });

    const record = await testClient.run(async (ctx: any) => ctx.db.get(recordId));
    expect(record.visibility).toBe("private");
  });

  test("throws when {{label}} not found", async ({ client, testClient }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Temp",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Temp",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: true,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });
    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    const recordId = records[0]._id;
    await testClient.run(async (ctx: any) => ctx.db.delete(recordId));

    await expect(
      client.mutation(api.{{name}}.mutations.assign, {
        {{camelCase name}}Id: recordId,
        assigneeId: undefined,
      }),
    ).rejects.toThrow("not found");
  });

  test("auto-flips visibility to shared when assigning to another user", async ({
    client,
    testClient,
  }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Assign test",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Assign test",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records[0].visibility).toBe("private");

    const otherUserId = await testClient.run(async (ctx: any) =>
      ctx.db.insert("users", { name: "Other User" }),
    );

    await client.mutation(api.{{name}}.mutations.assign, {
      {{camelCase name}}Id: records[0]._id,
      assigneeId: otherUserId,
    });

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(records[0]._id),
    );
    expect(updated.assigneeId).toBe(otherUserId);
    expect(updated.visibility).toBe("shared");
  });

  test("does not flip visibility when assigning to creator", async ({
    client,
    userId,
    testClient,
  }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Self-assign test",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Self-assign test",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );

    await client.mutation(api.{{name}}.mutations.assign, {
      {{camelCase name}}Id: records[0]._id,
      assigneeId: userId,
    });

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(records[0]._id),
    );
    expect(updated.assigneeId).toBe(userId);
    expect(updated.visibility).toBe("private");
  });

  test("unassigning flips visibility to shared", async ({
    client,
    testClient,
  }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Unassign test",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Unassign test",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });
    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );
    expect(records[0].visibility).toBe("private");

    await client.mutation(api.{{name}}.mutations.assign, {
      {{camelCase name}}Id: records[0]._id,
      assigneeId: undefined,
    });

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(records[0]._id),
    );
    expect(updated.assigneeId).toBeUndefined();
    expect(updated.visibility).toBe("shared");
  });
});
// @generated-end test-assign
{{/if}}

{{#if behaviors.orderable}}
// @generated-start test-reorder
describe("reorder", () => {
  test("does nothing when unauthenticated", async ({ testClient }) => {
    const recordId = await testClient.run(async (ctx: any) => {
      const userId = await ctx.db.insert("users", { name: "Seed" });
      return ctx.db.insert("{{name}}", {
{{#each fields}}
{{#ifEq this.type "string"}}
        {{@key}}: "Seed",
{{/ifEq}}
{{#ifEq this.type "text"}}
        {{@key}}: "Seed content",
{{/ifEq}}
{{#ifEq this.type "boolean"}}
        {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "number"}}
        {{@key}}: 0,
{{/ifEq}}
{{#ifEq this.type "enum"}}
        {{@key}}: "{{this.values.[0]}}",
{{/ifEq}}
{{/each}}
        creatorId: userId,
{{#if behaviors.assignable}}
        assigneeId: userId,
{{/if}}
        position: 1,
      });
    });

    await testClient.mutation(api.{{name}}.mutations.reorder, {
      {{camelCase name}}Id: recordId,
      newPosition: 999,
    });

    const record = await testClient.run(async (ctx: any) => ctx.db.get(recordId));
    expect(record.position).toBe(1);
  });

  test("updates position field", async ({ client, testClient }) => {
    await client.mutation(api.{{name}}.mutations.create, {
{{#each fields}}
{{#if this.required}}
{{#ifEq this.type "string"}}
      {{@key}}: "Reorder test",
{{/ifEq}}
{{#ifEq this.type "text"}}
      {{@key}}: "Reorder test",
{{/ifEq}}
{{#ifEq this.type "number"}}
      {{@key}}: 1,
{{/ifEq}}
{{#ifEq this.type "boolean"}}
      {{@key}}: false,
{{/ifEq}}
{{#ifEq this.type "enum"}}
{{#unless this.transitions}}
      {{@key}}: "{{this.values.[0]}}",
{{/unless}}
{{/ifEq}}
{{/if}}
{{/each}}
    });

    const records = await testClient.run(async (ctx: any) =>
      ctx.db.query("{{name}}").collect(),
    );

    await client.mutation(api.{{name}}.mutations.reorder, {
      {{camelCase name}}Id: records[0]._id,
      newPosition: 42,
    });

    const updated = await testClient.run(async (ctx: any) =>
      ctx.db.get(records[0]._id),
    );
    expect(updated.position).toBe(42);
  });
});
// @generated-end test-reorder
{{/if}}
