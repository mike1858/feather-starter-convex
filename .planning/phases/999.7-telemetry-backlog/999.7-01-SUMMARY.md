---
phase: 999.7-telemetry-backlog
plan: 01
subsystem: cli
tags: [telemetry, ndjson, commander, yaml, zod]

requires:
  - phase: 999.6-registry-backlog
    provides: feather CLI command patterns (add, remove, list)
provides:
  - bin/lib/telemetry.ts — NDJSON telemetry read/write utilities with opt-out and remote POST
  - bin/commands/stats.ts — feather stats CLI command for local telemetry analysis
  - projectYamlSchema telemetry and errorReporting configuration sections
affects: [999.7-02, 999.7-03]

tech-stack:
  added: []
  patterns: [NDJSON append-only telemetry file, fire-and-forget remote POST, sensitive data sanitization]

key-files:
  created:
    - bin/lib/telemetry.ts
    - bin/commands/stats.ts
    - bin/__tests__/telemetry.test.ts
    - bin/__tests__/stats.test.ts
  modified:
    - templates/schema/feather-yaml.schema.ts
    - templates/schema/__tests__/feather-yaml.test.ts
    - bin/feather.ts

key-decisions:
  - "NDJSON format for telemetry file (one JSON object per line, append-only)"
  - "Sensitive data sanitization via regex patterns on string fields"
  - "statsAction accepts projectRoot parameter for testability (temp dirs in tests)"

patterns-established:
  - "Telemetry opt-out: env var check first, then feather.yaml config"
  - "Fire-and-forget remote: catch all errors silently, never block CLI"

requirements-completed: [TEL-CLI-01, TEL-CLI-02, TEL-CLI-03, TEL-STATS-01, TEL-PRIVACY-01, TEL-CONFIG-01]

duration: 4min
completed: 2026-04-01
---

# Phase 999.7 Plan 01: CLI Telemetry Infrastructure Summary

**NDJSON telemetry library with opt-out, remote POST, sensitive data sanitization, feather stats command, and feather.yaml schema extension**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-01T10:06:38Z
- **Completed:** 2026-04-01T10:11:26Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Telemetry library (bin/lib/telemetry.ts) with NDJSON read/write, opt-out via env var and YAML config, fire-and-forget remote POST, sensitive data sanitization, and configurable telemetry levels
- feather stats command (bin/commands/stats.ts) reading local telemetry, aggregating by command/feature, with --days time range filtering and terminal-formatted output
- projectYamlSchema extended with telemetry (enabled, level, remote, errorDigest) and errorReporting (sentry.dsn) sections, fully backward-compatible
- 23 new tests (16 telemetry + 7 stats) plus 4 new schema tests, all passing alongside full 821-test suite

## Task Commits

Each task was committed atomically:

1. **Task 1: Telemetry library + feather.yaml schema extension**
   - `3db9c46` (test) — failing tests for telemetry library and schema extension
   - `424ae10` (feat) — telemetry library and feather.yaml schema extension

2. **Task 2: feather stats command + CLI telemetry wiring**
   - `df79712` (test) — failing tests for feather stats command
   - `025d2f0` (feat) — feather stats command and CLI telemetry wiring

## Files Created/Modified

- `bin/lib/telemetry.ts` — Telemetry read/write utilities (isEnabled, appendEvent, readEvents, postRemote, sanitizeEvent, getTelemetryLevel)
- `bin/commands/stats.ts` — feather stats CLI command (statsAction, formatStats, statsCommand)
- `bin/__tests__/telemetry.test.ts` — 16 tests for telemetry library
- `bin/__tests__/stats.test.ts` — 7 tests for stats command
- `templates/schema/feather-yaml.schema.ts` — Added telemetry and errorReporting sections to projectYamlSchema
- `templates/schema/__tests__/feather-yaml.test.ts` — 4 new tests for telemetry schema validation
- `bin/feather.ts` — Registered statsCommand

## Decisions Made

- Used NDJSON format (one JSON per line) for telemetry file — simple append-only, no need to parse entire file to add events
- Sensitive data sanitization uses regex replacement on string field values (password, token, secret, key, authorization patterns)
- statsAction takes projectRoot parameter for testability — tests use temp directories with real file I/O

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test data for "validate" event without feature**
- **Found during:** Task 2 (stats test GREEN phase)
- **Issue:** makeEvent helper defaults feature to "tasks", so `makeEvent({ command: "validate" })` still had feature: "tasks" instead of no feature
- **Fix:** Changed to `makeEvent({ command: "validate", feature: undefined })` to explicitly override the default
- **Files modified:** bin/__tests__/stats.test.ts
- **Verification:** Test now correctly asserts feature counts without counting the validate event
- **Committed in:** 025d2f0 (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Minor test data fix. No scope creep.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Telemetry infrastructure ready for Plans 02 and 03 (error capture, error dashboard)
- bin/lib/telemetry.ts exports are available for wiring into existing CLI commands (appendEvent after each command runs)
- projectYamlSchema telemetry section enables feather.yaml validation for telemetry configuration

## Self-Check: PASSED

- All 7 created/modified files verified on disk
- All 4 commit hashes verified in git history (3db9c46, 424ae10, df79712, 025d2f0)

---
*Phase: 999.7-telemetry-backlog*
*Plan: 01*
*Completed: 2026-04-01*
