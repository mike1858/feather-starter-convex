---
phase: 02.1-stripe-plugin-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/http.ts
  - convex/env.ts
  - convex/init.ts
  - convex/onboarding/mutations.ts
  - convex/users/mutations.ts
  - convex/users/queries.ts
  - convex/billing/stripe.ts
  - convex/billing/stripe.test.ts
  - convex/billing/actions.ts
  - convex/billing/queries.ts
  - convex/billing/queries.test.ts
  - convex/email/templates/subscriptionEmail.tsx
  - types.ts
  - site.config.ts
  - package.json
  - lefthook.yml
  - vitest.config.ts
  - src/shared/nav.ts
  - src/shared/errors.ts
  - src/shared/schemas/billing.ts
  - src/shared/schemas/billing.test.ts
  - src/i18n.ts
  - src/utils/misc.ts
  - src/features/billing/
  - src/features/onboarding/components/UsernamePage.tsx
  - src/features/onboarding/onboarding.test.tsx
  - src/features/dashboard/components/Navigation.tsx
  - src/routes/_app/_auth/dashboard/_layout.settings.billing.tsx
  - src/routes/_app/_auth/dashboard/_layout.settings.billing.test.tsx
  - src/routes/_app/_auth/dashboard/_layout.checkout.tsx
  - src/routes/_app/_auth/dashboard/_layout.checkout.test.tsx
  - public/locales/en/billing.json
  - public/locales/es/billing.json
  - e2e/settings.spec.ts
autonomous: true
requirements:
  - STRIPE-EXTRACT-01
must_haves:
  truths:
    - "Core runs without any Stripe env vars or SDK"
    - "No billing imports remain in core codebase"
    - "npm run dev starts without errors (predev script works with no-op init.ts)"
    - "All unit tests pass with 100% coverage"
    - "TypeScript compilation passes for all tsconfig files"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Schema with no billing tables (plans, subscriptions) and no customerId"
      contains: "devEmails"
    - path: "convex/http.ts"
      provides: "HTTP router with auth routes only"
      min_lines: 3
    - path: "convex/init.ts"
      provides: "No-op init action for predev script compatibility"
      min_lines: 3
    - path: "types.ts"
      provides: "Simplified User type without subscription join"
      contains: "Doc<\"users\">"
  key_links:
    - from: "convex/onboarding/mutations.ts"
      to: "convex/schema.ts"
      via: "completeOnboarding takes only username (no currency)"
      pattern: "args:.*username"
    - from: "convex/users/mutations.ts"
      to: "convex/schema.ts"
      via: "deleteCurrentUserAccount has no subscription lookup"
      pattern: "asyncMap.*password.*password-reset"
    - from: "convex/users/queries.ts"
      to: "types.ts"
      via: "getCurrentUser returns user + avatarUrl only (no subscription join)"
      pattern: "avatarUrl"
---

<objective>
Extract all Stripe/billing code from the core starter, producing a billing-free main branch.

Purpose: The Stripe SDK blocks team in India from running the starter. After extraction, core runs without Stripe API keys, no billing tables, no SDK dependency.
Output: A clean, billing-free core where all tests pass at 100% coverage.
</objective>

<execution_context>
@/Users/siraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/siraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-stripe-plugin-extraction/02.1-CONTEXT.md
@.planning/phases/02.1-stripe-plugin-extraction/02.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From types.ts (BEFORE -- will be simplified):
```typescript
export type User = Doc<"users"> & {
  avatarUrl?: string;
  subscription?: Doc<"subscriptions"> & {
    planKey: PlanKey;
  };
};
```

From types.ts (AFTER):
```typescript
export type User = Doc<"users"> & {
  avatarUrl?: string;
};
```

From convex/schema.ts (AFTER -- target state):
```typescript
import { defineSchema, defineTable } from "convex/server";
import { authTables } from "@convex-dev/auth/server";
import { v } from "convex/values";

const schema = defineSchema({
  ...authTables,
  users: defineTable({
    name: v.optional(v.string()),
    username: v.optional(v.string()),
    imageId: v.optional(v.id("_storage")),
    image: v.optional(v.string()),
    email: v.optional(v.string()),
    emailVerificationTime: v.optional(v.number()),
    phone: v.optional(v.string()),
    phoneVerificationTime: v.optional(v.number()),
    isAnonymous: v.optional(v.boolean()),
  }).index("email", ["email"]),
  devEmails: defineTable({
    to: v.array(v.string()),
    subject: v.string(),
    html: v.string(),
    text: v.optional(v.string()),
    sentAt: v.number(),
  }).index("sentAt", ["sentAt"]),
});

export default schema;
```

From convex/http.ts (AFTER):
```typescript
import { httpRouter } from "convex/server";
import { auth } from "./auth";

const http = httpRouter();
auth.addHttpRoutes(http);

export default http;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete billing files and surgically edit core files</name>
  <files>
    convex/billing/stripe.ts, convex/billing/stripe.test.ts, convex/billing/actions.ts,
    convex/billing/queries.ts, convex/billing/queries.test.ts,
    convex/email/templates/subscriptionEmail.tsx,
    src/features/billing/ (entire directory),
    src/shared/schemas/billing.ts, src/shared/schemas/billing.test.ts,
    src/routes/_app/_auth/dashboard/_layout.settings.billing.tsx,
    src/routes/_app/_auth/dashboard/_layout.settings.billing.test.tsx,
    src/routes/_app/_auth/dashboard/_layout.checkout.tsx,
    src/routes/_app/_auth/dashboard/_layout.checkout.test.tsx,
    public/locales/en/billing.json, public/locales/es/billing.json,
    convex/schema.ts, convex/http.ts, convex/env.ts, convex/init.ts,
    convex/onboarding/mutations.ts, convex/users/mutations.ts, convex/users/queries.ts,
    types.ts, site.config.ts, package.json,
    src/shared/nav.ts, src/shared/errors.ts, src/i18n.ts, src/utils/misc.ts,
    src/features/onboarding/components/UsernamePage.tsx,
    src/features/dashboard/components/Navigation.tsx,
    e2e/settings.spec.ts
  </files>
  <action>
    **Phase A -- Delete billing files entirely (rm -rf / git rm):**
    - `convex/billing/` (entire directory -- stripe.ts, stripe.test.ts, actions.ts, queries.ts, queries.test.ts)
    - `convex/email/templates/subscriptionEmail.tsx`
    - `src/features/billing/` (entire directory)
    - `src/shared/schemas/billing.ts` and `src/shared/schemas/billing.test.ts`
    - `src/routes/_app/_auth/dashboard/_layout.settings.billing.tsx` and its `.test.tsx`
    - `src/routes/_app/_auth/dashboard/_layout.checkout.tsx` and its `.test.tsx`
    - `public/locales/en/billing.json` and `public/locales/es/billing.json`

    **Phase B -- Surgical edits to core files:**

    1. **convex/schema.ts**: Remove ALL billing exports (CURRENCIES, INTERVALS, PLANS, validators, types, priceValidator, pricesValidator). Remove `plans` and `subscriptions` table definitions. Remove `customerId` field and its `.index("customerId", ...)` from users table. Remove billing Zod schema imports. Remove `zodToConvex` import (no longer needed -- no Zod schemas left in schema.ts). Remove `Infer` from convex/values import (no longer used). Final result: just `defineSchema, defineTable` from convex/server, `authTables` from @convex-dev/auth/server, `v` from convex/values. Two tables: users (with email index only) and devEmails (with sentAt index).

    2. **convex/http.ts**: Replace entire file with just httpRouter + auth routes (3 lines of logic). Remove ALL Stripe imports, webhook handlers, event processing.

    3. **convex/env.ts**: Remove `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` lines. Keep AUTH_RESEND_KEY, AUTH_EMAIL, HOST_URL, SITE_URL.

    4. **convex/init.ts**: Replace entire file with no-op internalAction so predev script still works:
       ```typescript
       import { internalAction } from "@cvx/_generated/server";
       export default internalAction(async () => {
         // No-op. Plugin/billing overrides this with Stripe product seeding.
       });
       ```
       Remove `insertSeedPlan` mutation export entirely.

    5. **convex/onboarding/mutations.ts**: Remove `currency` from zMutation args (keep only `username`). Remove `import { currency as currencyZod } from "../../src/shared/schemas/billing"`. Remove the entire `if (user.customerId)` block and the `ctx.scheduler.runAfter(0, internal.billing.stripe.PREAUTH_createStripeCustomer, ...)` call. Remove `import { internal }` (no longer needed). Simplified handler: just `auth.getUserId`, `ctx.db.get`, `ctx.db.patch(userId, { username: args.username })`.

    6. **convex/users/mutations.ts**: In `deleteCurrentUserAccount`, remove the entire subscription lookup + deletion + Stripe cancellation block (lines 58-70). Remove `import { internal }` (no longer needed). Add "password" and "password-reset" to the auth providers list in asyncMap (currently only has "resend-otp" and "github" -- Phase 2 added these providers but the list was not updated). The handler should: get userId, get user, throw if !user, delete user, then asyncMap over ["resend-otp", "github", "password", "password-reset"] to clean up auth accounts.

    7. **convex/users/queries.ts**: Simplify `getCurrentUser` to remove subscription/plan join. Remove `Promise.all` -- just `ctx.db.get(userId)`. Return `{ ...user, avatarUrl: avatarUrl || undefined }` with no subscription property. Update return type annotation from `Promise<User | undefined>` -- the User type will already be simplified. Remove `User` import from `~/types` since the simplified function does not need an explicit type annotation (Convex infers return types).

    8. **types.ts**: Simplify to `Doc<"users"> & { avatarUrl?: string }`. Remove `PlanKey` import. Remove subscription property from User type.

    9. **src/shared/nav.ts**: Remove the Billing nav item object (the one with `to: "/dashboard/settings/billing"`). Keep Dashboard, Settings, and Dev Mailbox entries.

    10. **src/shared/errors.ts**: Remove the entire `billing: { ... }` group. Keep auth, onboarding, common groups and the plugin comment.

    11. **src/i18n.ts**: Remove `"billing"` from the `ns` array. Result: `["common", "auth", "dashboard", "settings", "onboarding"]`.

    12. **src/utils/misc.ts**: Remove `getLocaleCurrency` function and the `CURRENCIES` import from `@cvx/schema`. Keep `cn`, `callAll`, `useSignOut`. Also remove the unused `useAuthActions` import if present (check -- it is used by useSignOut, so keep it).

    13. **src/features/onboarding/components/UsernamePage.tsx**: Remove `import { getLocaleCurrency } from "@/utils/misc"`. Remove `currency: getLocaleCurrency()` from the `completeOnboarding` mutation call. The onSubmit should just pass `{ username: value.username }`.

    14. **src/features/dashboard/components/Navigation.tsx**: Remove `import { PLANS } from "@cvx/schema"` (PLANS no longer exists). Remove `const BILLING_PATH`. Remove the plan badge span (lines 71-76 -- the `<span>` showing "Pro"/"Free" next to username). Remove the "Upgrade to PRO" conditional block (lines 110-125). Keep everything else (avatar, username, dropdown, settings link, nav tabs).

    15. **site.config.ts**: Change siteDescription to remove "Stripe" reference. Use: "A lightweight, production-ready starter template powered by Convex and React."

    16. **package.json**: Remove `"stripe": "^16.6.0"` from dependencies. Remove `"predev": "convex dev --run init --until-success"` script (init.ts is now no-op, running it wastes startup time). Actually, KEEP predev -- the no-op runs in <1s and ensures plugin/billing can override it. Per research recommendation.

    17. **e2e/settings.spec.ts**: Remove the "billing page loads" test (the one visiting `/dashboard/settings/billing`). Keep the "profile settings page loads with username" test.

    **Phase C -- Update onboarding test file:**
    - Read `src/features/onboarding/onboarding.test.tsx` and remove any `currency` references from `completeOnboarding` mutation calls in tests.
    - Also update `src/features/onboarding/README.md` to remove currency/Stripe references.

    **Phase D -- Run npm install to update lockfile after removing stripe dependency:**
    ```bash
    npm install
    ```
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && npx tsc -p tsconfig.app.json --noEmit && npx tsc -p tsconfig.node.json --noEmit</automated>
  </verify>
  <done>All billing files deleted, all surgical edits complete, TypeScript compiles without errors across all tsconfig files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix tests, coverage, and lefthook configuration</name>
  <files>
    vitest.config.ts, lefthook.yml
  </files>
  <action>
    **Step 1 -- Run tests to see what breaks:**
    ```bash
    npx vitest run
    ```
    Fix any remaining broken imports or test failures caused by billing removal. Common issues:
    - Tests importing from deleted billing files
    - Tests referencing `subscription` property on User
    - Coverage include/exclude paths referencing deleted files

    **Step 2 -- Update vitest.config.ts coverage config:**
    - The `convex/http.ts` exclusion can potentially be REMOVED from the exclude list since http.ts is now trivially small (3 lines). However, it has no testable logic, so keeping it excluded is fine. Keep it excluded for simplicity.
    - `convex/init.ts` exclusion -- keep it (no-op, not worth testing).
    - Remove any billing-specific paths that no longer exist (they will be harmlessly ignored, but clean them up).

    **Step 3 -- Test if dangerouslyIgnoreUnhandledErrors can be removed:**
    Run `npx vitest run --coverage` WITHOUT the flag. If it passes clean, update `lefthook.yml`:
    - Remove `--dangerouslyIgnoreUnhandledErrors` from the test-coverage command
    - Result: `run: npx vitest run --coverage`

    **Step 4 -- Test if convex/tsconfig.json typecheck can be re-enabled:**
    Run `npx tsc -p convex/tsconfig.json --noEmit`. If it passes (the TS2554 error was from http.ts Stripe code which is now gone), update `lefthook.yml`:
    - Re-add convex typecheck: `run: npx tsc -p tsconfig.app.json --noEmit && npx tsc -p tsconfig.node.json --noEmit && npx tsc -p convex/tsconfig.json --noEmit`
    - Update the NOTE comment to reflect this is now passing

    **Step 5 -- Verify full coverage at 100%:**
    ```bash
    npx vitest run --coverage
    ```
    If coverage drops below 100%, investigate -- likely a file that was previously excluded (because it was billing-related) is now being included but has no tests. Adjust the exclude list in vitest.config.ts as needed.

    **Step 6 -- Run the typecheck script:**
    ```bash
    npm run typecheck
    ```
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && npx vitest run --coverage && npm run typecheck</automated>
  </verify>
  <done>All tests pass with 100% coverage. lefthook.yml updated (dangerouslyIgnoreUnhandledErrors removed if safe, convex typecheck re-enabled if passing). npm run typecheck passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --coverage` -- all tests pass, 100% coverage
2. `npm run typecheck` -- all three tsconfig files pass
3. `grep -r "stripe\|STRIPE\|billing\|BILLING\|subscription\|PLANS\|CURRENCIES\|INTERVALS\|PlanKey\|customerId" --include="*.ts" --include="*.tsx" convex/ src/ types.ts` -- no billing references remain (except plugin comments in errors.ts and possibly test infrastructure)
4. `npm run dev` -- starts without errors (predev no-op runs fine)
</verification>

<success_criteria>
- Zero billing/Stripe imports in core codebase
- npm test passes with 100% coverage
- TypeScript compiles cleanly
- package.json has no stripe dependency
- convex/schema.ts has only users and devEmails tables
- Navigation shows no plan badge or upgrade button
- Onboarding takes only username (no currency)
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-stripe-plugin-extraction/02.1-01-SUMMARY.md`
</output>
