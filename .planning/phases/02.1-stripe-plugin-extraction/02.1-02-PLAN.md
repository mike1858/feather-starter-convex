---
phase: 02.1-stripe-plugin-extraction
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - "(git branch operations -- no file modifications on main)"
autonomous: false
requirements:
  - STRIPE-EXTRACT-02
must_haves:
  truths:
    - "plugin/billing branch exists and merging it into billing-free main restores billing functionality"
    - "All 3 existing plugin branches (infra-ci, command-palette, admin-panel) work with billing-free main"
    - "Each rebased plugin branch passes npm test when merged into main"
  artifacts:
    - path: "(branch) plugin/billing"
      provides: "Complete billing feature as a plugin branch"
    - path: "(branch) plugin/infra-ci-github-actions"
      provides: "CI plugin compatible with billing-free main"
    - path: "(branch) plugin/ui-command-palette"
      provides: "Command palette plugin compatible with billing-free main"
    - path: "(branch) plugin/feature-admin-panel"
      provides: "Admin panel plugin compatible with billing-free main"
  key_links:
    - from: "plugin/billing (branch)"
      to: "main (branch)"
      via: "git merge adds billing back to core"
      pattern: "convex/billing/stripe.ts exists after merge"
    - from: "plugin/infra-ci-github-actions (branch)"
      to: "main (branch)"
      via: "git merge adds CI without billing conflicts"
---

<objective>
Create plugin/billing branch from pre-extraction code and update all 3 existing plugin branches to be compatible with the billing-free main.

Purpose: Complete the plugin extraction pattern -- billing becomes optional, existing plugins stay functional.
Output: plugin/billing branch + 3 rebased/updated plugin branches.
</objective>

<execution_context>
@/Users/siraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/siraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-stripe-plugin-extraction/02.1-CONTEXT.md
@.planning/phases/02.1-stripe-plugin-extraction/02.1-RESEARCH.md
@.planning/phases/02.1-stripe-plugin-extraction/02.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin/billing branch</name>
  <files>(git branch operations)</files>
  <action>
    **Strategy (from RESEARCH.md Option B):** Branch from billing-free main, then add billing code as new commits. This ensures the plugin diff is purely additive against current main.

    **Step 1 -- Identify pre-extraction commit:**
    ```bash
    # Find the last commit before Plan 01 extraction work
    git log --oneline -20
    ```
    Note the commit hash of the last commit BEFORE the extraction began (should be the Plan 01 commit or the commit just before it).

    **Step 2 -- Create plugin/billing branch from current (billing-free) main:**
    ```bash
    git checkout -b plugin/billing main
    ```

    **Step 3 -- Copy billing files from pre-extraction state:**
    Use `git show <pre-extraction-commit>:<filepath>` to extract each billing file and write it to the branch. Files to add:

    Backend files:
    - `convex/billing/stripe.ts` -- full Stripe SDK integration
    - `convex/billing/stripe.test.ts` -- billing backend tests
    - `convex/billing/actions.ts` -- checkout/portal actions
    - `convex/billing/queries.ts` -- getActivePlans query
    - `convex/billing/queries.test.ts` -- billing query tests
    - `convex/email/templates/subscriptionEmail.tsx` -- billing email template

    Schema/config files (MODIFY, not create):
    - `convex/schema.ts` -- restore billing tables (plans, subscriptions), customerId, billing constants/validators/types
    - `convex/http.ts` -- restore Stripe webhook handler
    - `convex/env.ts` -- restore STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET
    - `convex/init.ts` -- restore Stripe product seeding (replace no-op)
    - `convex/onboarding/mutations.ts` -- restore currency arg + createStripeCustomer scheduler
    - `convex/users/mutations.ts` -- restore subscription cancellation in deleteCurrentUserAccount
    - `convex/users/queries.ts` -- restore subscription/plan join in getCurrentUser
    - `types.ts` -- restore full User type with subscription join

    Frontend files:
    - `src/features/billing/` -- entire directory from pre-extraction
    - `src/shared/schemas/billing.ts` + test
    - `src/routes/_app/_auth/dashboard/_layout.settings.billing.tsx` + test
    - `src/routes/_app/_auth/dashboard/_layout.checkout.tsx` + test
    - `public/locales/en/billing.json`
    - `public/locales/es/billing.json`

    Extension points (MODIFY -- append billing entries):
    - `src/shared/nav.ts` -- add Billing nav item back
    - `src/shared/errors.ts` -- add billing error group back
    - `src/i18n.ts` -- add "billing" to ns array
    - `src/utils/misc.ts` -- add getLocaleCurrency back
    - `src/features/onboarding/components/UsernamePage.tsx` -- restore currency in mutation call
    - `src/features/dashboard/components/Navigation.tsx` -- restore plan badge + upgrade button
    - `site.config.ts` -- restore Stripe mention in description
    - `package.json` -- add stripe dependency back
    - `e2e/settings.spec.ts` -- restore billing E2E test

    **Step 4 -- Commit billing additions:**
    ```bash
    git add -A
    git commit -m "feat(plugin/billing): add Stripe billing as optional plugin

    Adds complete billing feature: Stripe integration, subscription management,
    checkout flow, billing settings, webhook handling, and product seeding.
    Install with: scripts/plugin.sh install plugin/billing"
    ```

    **Step 5 -- Verify plugin works when merged:**
    ```bash
    git checkout -b test-billing-merge main
    git merge plugin/billing --no-edit
    npm install
    npx tsc -p tsconfig.app.json --noEmit
    # Note: full test suite may not pass without Stripe env vars, but typecheck should pass
    ```

    **Step 6 -- Clean up test branch and return to main:**
    ```bash
    git checkout main
    git branch -D test-billing-merge
    ```
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && git branch --list "plugin/billing" | grep -q "plugin/billing" && echo "plugin/billing branch exists" && git checkout -b test-billing-merge-verify main && git merge plugin/billing --no-edit && npx tsc -p tsconfig.app.json --noEmit && git checkout main && git branch -D test-billing-merge-verify && echo "merge-and-compile verified"</automated>
  </verify>
  <done>plugin/billing branch exists, diverges from billing-free main, contains all billing code as additive commits. Merging into main restores billing functionality (TypeScript compiles after merge).</done>
</task>

<task type="auto">
  <name>Task 2: Rebase existing plugin branches onto billing-free main</name>
  <files>(git branch operations)</files>
  <action>
    For each of the 3 existing plugin branches, rebase onto the new billing-free main. The plugins forked from a billing-included main, so their diffs may contain billing code in shared files.

    **For each branch (plugin/infra-ci-github-actions, plugin/ui-command-palette, plugin/feature-admin-panel):**

    **Step 1 -- Rebase onto new main:**
    ```bash
    git checkout plugin/<name>
    git rebase main
    ```

    **Step 2 -- Resolve conflicts:**
    Conflicts will likely occur in shared extension point files:
    - `src/shared/nav.ts` -- resolve by keeping the plugin's OWN additions but dropping billing entries
    - `src/shared/errors.ts` -- same: keep plugin additions, drop billing group
    - `src/i18n.ts` -- keep plugin namespace, drop "billing"
    - `convex/schema.ts` -- keep plugin additions, drop billing tables/constants
    - `package.json` -- keep plugin dependencies, drop stripe

    For each conflict, the resolution principle is: **accept main's version (billing-free) as the base, then re-apply only the plugin's own additions.**

    **Step 3 -- Verify each rebased plugin:**
    ```bash
    git checkout -b test-plugin-merge main
    git merge plugin/<name> --no-edit
    npm install
    npx vitest run
    npx tsc -p tsconfig.app.json --noEmit
    ```
    If tests pass and typecheck passes, the rebase is good.

    **Step 4 -- Clean up test branch:**
    ```bash
    git checkout main
    git branch -D test-plugin-merge
    ```

    **Repeat for all 3 plugins.** If a plugin has zero billing-related changes in its diff (possible for infra-ci which is CI-only), the rebase may be clean with no conflicts.
  </action>
  <verify>
    <automated>cd /Users/siraj/Desktop/NonDropBoxProjects/feather-starter-convex && for branch in plugin/infra-ci-github-actions plugin/ui-command-palette plugin/feature-admin-panel; do git log --oneline -1 "$branch" 2>/dev/null && echo "$branch OK" || echo "$branch MISSING"; done</automated>
  </verify>
  <done>All 3 existing plugin branches rebased onto billing-free main. Each can be merged into main without conflicts and passes npm test + typecheck after merge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify billing-free dashboard and plugin merge</name>
  <files>(no files -- visual verification)</files>
  <action>
    User verifies the complete Stripe plugin extraction:
    1. Core codebase is billing-free (Plan 01)
    2. plugin/billing branch created with all billing code
    3. Three existing plugin branches updated for compatibility
  </action>
  <verify>
    1. Run `npm run dev` -- verify dashboard loads with no billing UI (no plan badge, no upgrade button, no billing nav tab)
    2. Visit `/dashboard/settings` -- confirm billing tab is gone, profile settings work
    3. Run `npm test` -- confirm 100% coverage
    4. Optionally test plugin merge: `git checkout -b test main && git merge plugin/billing` -- verify billing UI reappears
  </verify>
  <done>User confirms billing-free dashboard renders correctly and plugin merge restores billing UI.</done>
</task>

</tasks>

<verification>
1. `git branch --list "plugin/billing"` -- branch exists
2. `npm test` -- passes on main (billing-free)
3. `git stash && git checkout -b verify-billing main && git merge plugin/billing --no-edit && npx tsc -p tsconfig.app.json --noEmit && git checkout main && git branch -D verify-billing && git stash pop` -- billing plugin merges and compiles
4. For each existing plugin: merge test branch passes `npx vitest run`
</verification>

<success_criteria>
- plugin/billing branch exists with all billing code as additive diff
- All 3 existing plugin branches rebased and functional on billing-free main
- Each plugin can be independently merged into main without conflicts
- User visually confirms billing-free dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-stripe-plugin-extraction/02.1-02-SUMMARY.md`
</output>
