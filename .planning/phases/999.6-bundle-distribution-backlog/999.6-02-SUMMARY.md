---
phase: 999.6-bundle-distribution-backlog
plan: 02
subsystem: cli
tags: [commander, bundle, dependency-resolution, topo-sort, feather-cli]

requires:
  - phase: 999.6-01
    provides: resolve module, topo-sort module, bundle manifest schema, templates/features rename
provides:
  - Bundle-aware feather add with dependency-ordered installation
  - Bundle-aware feather remove with multi-feature removal
  - feather list command showing installed/available features and bundles
affects: [999.6-03, 999.6-bundle-distribution-backlog]

tech-stack:
  added: []
  patterns:
    - "installSingleFeature/removeSingleFeature helpers for reuse by bundle path"
    - "resolve() as entry point for bundle/feature auto-detection in all commands"
    - "D-10 pattern: force-update already-installed features during bundle install"
    - "D-11 pattern: validateDependencies before install, clear error for missing external deps"

key-files:
  created:
    - bin/commands/list.ts
    - bin/__tests__/list.test.ts
  modified:
    - bin/commands/add.ts
    - bin/commands/remove.ts
    - bin/feather.ts
    - bin/__tests__/add.test.ts
    - bin/__tests__/remove.test.ts

key-decisions:
  - "Bundle install uses force=true for all features (per D-10 update policy)"
  - "Not-found error message includes both features AND bundles for discovery"
  - "Bundle removal is partial-tolerant: removes what's installed, skips missing"

patterns-established:
  - "resolve() → branch on type → installSingleFeature/removeSingleFeature pattern for CLI commands"

requirements-completed: [D-07, D-10]

duration: 5min
completed: 2026-04-01
---

# Phase 999.6 Plan 02: CLI Bundle Commands Summary

**Bundle-aware feather add/remove with dependency ordering and feather list command for discovery**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-01T09:26:28Z
- **Completed:** 2026-04-01T09:32:20Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- feather add auto-detects bundles vs features, installs all bundle features in topological dependency order
- feather remove auto-detects bundles, removes all features in the bundle (partial-tolerant)
- feather list shows installed features, available features (not installed), and available bundles with installed status
- All 15 new tests pass (7 bundle add + 3 bundle remove + 7 list + backward compat preserved)
- D-10 (update already-installed) and D-11 (missing dependency error) both implemented

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend feather add/remove for bundle support** - `295d97c` (feat)
2. **Task 2: Create feather list command and wire into CLI** - `3ae28d7` (feat)

## Files Created/Modified
- `bin/commands/add.ts` — Bundle-aware addAction with resolve(), topoSort, validateDependencies
- `bin/commands/remove.ts` — Bundle-aware removeAction with resolve()
- `bin/commands/list.ts` — New listAction showing installed/available/bundles
- `bin/feather.ts` — Wired listCommand into CLI
- `bin/__tests__/add.test.ts` — 7 new bundle tests + refactored helpers
- `bin/__tests__/remove.test.ts` — 3 new bundle tests + refactored helpers
- `bin/__tests__/list.test.ts` — 7 new tests for list command

## Decisions Made
- Bundle install uses `force: true` for every feature, ensuring already-installed features get updated (per D-10) rather than skipped
- Not-found error message includes both features AND bundles so users can discover bundles
- Bundle removal is partial-tolerant: if some features aren't installed, it removes what it can and succeeds

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing test failures in `bin/__tests__/import.test.ts` (3 tests, from 999.5 Excel import phase) — confirmed present on main, not caused by this plan's changes. Out of scope per deviation rules.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All Plan 01 foundations (resolve, topo-sort, bundle manifest) are now wired into user-facing commands
- Plan 03 (registry sync + feather update extension) can proceed — the local-first commands are complete

## Self-Check: PASSED

All 7 source files exist. Both task commits (295d97c, 3ae28d7) verified. SUMMARY.md present.

---
*Phase: 999.6-bundle-distribution-backlog*
*Completed: 2026-04-01*
