---
phase: 03.2-crud-generator-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - templates/feature/page.tsx.hbs
  - templates/feature/title-bar.tsx.hbs
  - templates/feature/item.tsx.hbs
  - templates/feature/form.tsx.hbs
  - templates/feature/status-badge.tsx.hbs
  - templates/feature/view-switcher.tsx.hbs
  - templates/feature/empty-state.tsx.hbs
  - templates/feature/filter-bar.tsx.hbs
  - templates/feature/index.ts.hbs
  - templates/feature/test.tsx.hbs
autonomous: true
requirements: [GEN-01, GEN-04, GEN-05]

must_haves:
  truths:
    - "Page template produces a React component with title bar, filter bar, view switcher, and active view rendering"
    - "Item template produces a list item with drag handle, status badge, title, metadata, actions following the premium design spec"
    - "Form template produces a TanStack Form with field types mapped to correct input components"
    - "Status badge template produces a clickable dot-indicator badge with color-coded enum values"
    - "All templates use premium CSS tokens (shadow-card, surface-secondary, Inter Variable) from the design system"
    - "Frontend test template produces tests with renderWithRouter and userEvent matching the tasks gold standard"
  artifacts:
    - path: "templates/feature/page.tsx.hbs"
      provides: "Main page component template"
      contains: "TitleBar"
    - path: "templates/feature/item.tsx.hbs"
      provides: "List item component template"
      contains: "shadow-card"
    - path: "templates/feature/form.tsx.hbs"
      provides: "Create/edit form template"
      contains: "useForm"
    - path: "templates/feature/status-badge.tsx.hbs"
      provides: "Enum status badge template"
      contains: "rounded-full"
    - path: "templates/feature/test.tsx.hbs"
      provides: "Frontend test template"
      contains: "renderWithRouter"
  key_links:
    - from: "templates/feature/page.tsx.hbs"
      to: "templates/feature/title-bar.tsx.hbs"
      via: "import TitleBar"
      pattern: "import.*TitleBar"
    - from: "templates/feature/page.tsx.hbs"
      to: "templates/feature/view-switcher.tsx.hbs"
      via: "import ViewSwitcher"
      pattern: "import.*ViewSwitcher"
    - from: "templates/feature/test.tsx.hbs"
      to: "templates/feature/page.tsx.hbs"
      via: "import Page component"
      pattern: "import.*Page"
---

<objective>
Create all core frontend Handlebars templates: Page, TitleBar, Item, Form, StatusBadge, ViewSwitcher, EmptyState, FilterBar, barrel export, and frontend test. These templates produce premium React components matching the UI spec.

Purpose: Frontend templates transform YAML feature definitions into demoable React components. The premium design treatment (shadows, transitions, Inter font, dot-indicator badges) makes generated output look "designed" not "scaffolded."

Output: 10 Handlebars template files in `templates/feature/` for core frontend component generation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-crud-generator-upgrade/03.2-CONTEXT.md
@.planning/phases/03.2-crud-generator-upgrade/03.2-UI-SPEC.md

<interfaces>
From generators/utils/types.ts (created in Plan 01):
- FeatureConfig: { name, label, labelPlural, fields, timestamps, behaviors, access, views, operations }
- FieldConfig: { type, required, max, default, values, colors, transitions, showOn, hideOn }
- ViewConfig: { default: { defaultView, enabledViews }, list?, card?, table?, detail? }

From generators/utils/handlebars-helpers.js (created in Plan 01):
- formInputType(field) — returns React component name for form field
- enumBadgeClasses(field, value) — returns dot color + text color CSS classes
- ifEq(a, b, options) — equality conditional
- ifIn(value, array, options) — array membership
- showOnView(field, viewName) — view visibility check
- json(obj) — JSON.stringify
</interfaces>

@src/features/tasks/components/TasksPage.tsx
@src/features/tasks/components/TaskItem.tsx
@src/features/tasks/components/TaskForm.tsx
@src/features/tasks/components/TaskStatusBadge.tsx
@src/features/tasks/tasks.test.tsx
@src/features/tasks/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page, TitleBar, ViewSwitcher, FilterBar, EmptyState templates</name>
  <files>
    templates/feature/page.tsx.hbs, templates/feature/title-bar.tsx.hbs,
    templates/feature/view-switcher.tsx.hbs, templates/feature/filter-bar.tsx.hbs,
    templates/feature/empty-state.tsx.hbs
  </files>
  <read_first>
    src/features/tasks/components/TasksPage.tsx (gold standard page structure),
    .planning/phases/03.2-crud-generator-upgrade/03.2-UI-SPEC.md (Page Component, Collection Title Bar, View Switcher, Filter Bar, Empty State sections — read full spec for these),
    .planning/phases/03.2-crud-generator-upgrade/03.2-CONTEXT.md (D-22 through D-24 — view types, pagination, filtered views)
  </read_first>
  <action>
    Create 5 Handlebars templates following the UI spec contracts. All templates use triple-stache `{{{` for code output and `@generated-start`/`@generated-end` + `@custom-start`/`@custom-end` markers.

    **1. `templates/feature/page.tsx.hbs` — Main page component (per UI spec "Page Component")**

    Structure:
    ```
    // @generated-start imports
    import { useSuspenseQuery } from "@tanstack/react-query";
    import { convexQuery } from "@convex-dev/react-query";
    import { api } from "~/convex/_generated/api";
    import { useTranslation } from "react-i18next";
    import { useState } from "react";
    import { {{pascalCase name}}TitleBar } from "./{{pascalCase name}}TitleBar";
    import { {{pascalCase name}}Form } from "./{{pascalCase name}}Form";
    {{#ifEq views.default.enabledViews.length 1}}
    import { {{pascalCase name}}ListView } from "./{{pascalCase name}}ListView";
    {{else}}
    import { {{pascalCase name}}ViewSwitcher } from "./{{pascalCase name}}ViewSwitcher";
    // Dynamic view imports for each enabled view type
    {{/ifEq}}
    {{#if views.filteredViews}}
    import { {{pascalCase name}}FilterBar } from "./{{pascalCase name}}FilterBar";
    {{/if}}
    import { {{pascalCase name}}EmptyState } from "./{{pascalCase name}}EmptyState";
    // @generated-end imports
    // @custom-start imports
    // @custom-end imports

    export function {{pascalCase name}}Page() {
      const { t } = useTranslation("{{name}}");
      const { data } = useSuspenseQuery(convexQuery(api.{{name}}.queries.list, {}));
      // @generated-start state
      const [activeView, setActiveView] = useState<string>(
        () => localStorage.getItem("{{name}}-view-preference") || "{{views.default.defaultView}}"
      );
      {{#if views.filteredViews}}
      const [activeFilter, setActiveFilter] = useState<string>("all");
      {{/if}}
      // @generated-end state
      // @custom-start state
      // @custom-end state

      // Layout: flex flex-col gap-6, full width, max-w-5xl
      return (
        <div className="flex flex-col gap-6 w-full max-w-5xl">
          <{{pascalCase name}}TitleBar
            activeView={activeView}
            onViewChange={setActiveView}
          />
          {{#if operations.create}}
          <{{pascalCase name}}Form mode="inline" />
          {{/if}}
          {{#if views.filteredViews}}
          <{{pascalCase name}}FilterBar activeFilter={activeFilter} onFilterChange={setActiveFilter} />
          {{/if}}
          {/* View rendering based on active view */}
          {/* @custom-start content */}
          {/* @custom-end content */}
        </div>
      );
    }
    ```

    Handle view switching: when multiple views enabled, render the active view component. When single view, render it directly. Use `React.lazy` or conditional rendering.

    **2. `templates/feature/title-bar.tsx.hbs` — Collection title bar (per UI spec "Collection Title Bar")**

    Layout: `flex items-center justify-between gap-4 mb-6`
    - Left: `flex flex-col gap-1` with `h1` (`text-xl font-semibold tracking-tight text-primary`) and description (`text-sm text-primary/60`)
    - Right: `flex items-center gap-2` with search input (if `search: true`), view switcher (if multiple views), primary CTA button (`Button size="sm"`)
    - Search input: `relative` container with `Search` icon from lucide-react, `w-64 h-8 text-xs pl-8 pr-4 rounded-md border border-border bg-background`, debounce at 300ms
    - Uses `useTranslation("{{name}}")` for all copy

    **3. `templates/feature/view-switcher.tsx.hbs` — View type switcher (per UI spec "View Switcher")**

    Container: `inline-flex items-center gap-0.5 rounded-lg border border-border p-1 bg-muted/50`
    - One button per enabled view type from `views.default.enabledViews`
    - Active: `bg-background text-primary shadow-sm rounded-md`
    - Inactive: `text-muted-foreground hover:text-primary`
    - Icons from lucide-react: `List` for list, `LayoutGrid` for card, `Table2` for table, `Image` for cover
    - `px-4 py-1.5 rounded-md text-xs font-semibold transition-all duration-200`
    - Persist selection to `localStorage` key `{{name}}-view-preference`
    - Icons only on mobile (`< sm`), icon + label on desktop (`ml-1.5`)

    **4. `templates/feature/filter-bar.tsx.hbs` — Filter controls (per UI spec "Filter Bar")**

    Only generated when `views.filteredViews` is defined in YAML.
    - Tab-style filter: `flex items-center gap-0.5 border-b border-border`
    - "All" tab always first
    - Each filtered view as a tab with underline indicator for active state
    - Active tab: `text-primary` with `absolute bottom-0 left-0 right-0 h-0.5 bg-primary rounded-full`
    - Inactive tab: `text-muted-foreground hover:text-primary`
    - `px-4 py-2 text-sm font-semibold transition-colors duration-150 relative`

    **5. `templates/feature/empty-state.tsx.hbs` — Empty state (per UI spec "Empty State Component")**

    Container: `flex flex-col items-center justify-center py-16 px-8`
    - Icon: `h-12 w-12 text-primary/20 mb-4` — defaults by context: `List` (list), `LayoutGrid` (card), `Table2` (table), `Search` (filtered), `AlertCircle` (error)
    - Heading: `text-sm font-semibold text-primary mb-1`
    - Description: `text-sm text-primary/50 text-center max-w-sm mb-4`
    - CTA: `Button variant="outline" size="sm"` (optional, for "no data" states)
    - Three variants: `noData` (with CTA "Create {Label}"), `noMatches` (no CTA), `error` (with "Retry" CTA)
    - All copy from i18n translations
  </action>
  <verify>
    <automated>grep -q "useSuspenseQuery" templates/feature/page.tsx.hbs && grep -q "useForm" templates/feature/form.tsx.hbs 2>/dev/null; grep -q "useTranslation" templates/feature/page.tsx.hbs && grep -q "localStorage" templates/feature/view-switcher.tsx.hbs && grep -q "border-b border-border" templates/feature/filter-bar.tsx.hbs && grep -q "py-16 px-8" templates/feature/empty-state.tsx.hbs && echo "All 5 core frontend templates exist with key content"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/feature/page.tsx.hbs` contains `useSuspenseQuery`, `convexQuery`, `api.{{name}}.queries.list`, `useTranslation("{{name}}")`, `max-w-5xl`, `@generated-start imports`, `@custom-start imports`
    - `templates/feature/title-bar.tsx.hbs` contains `text-xl font-semibold tracking-tight text-primary`, `text-sm text-primary/60`, `Button size="sm"`, `flex items-center justify-between gap-4 mb-6`
    - `templates/feature/view-switcher.tsx.hbs` contains `localStorage`, `bg-muted/50`, `bg-background text-primary shadow-sm`, lucide icon imports (`List`, `LayoutGrid`, `Table2`)
    - `templates/feature/filter-bar.tsx.hbs` contains `border-b border-border`, `h-0.5 bg-primary rounded-full`, `text-muted-foreground hover:text-primary`
    - `templates/feature/empty-state.tsx.hbs` contains `py-16 px-8`, `text-primary/20`, `text-primary/50`, three variants (noData, noMatches, error)
  </acceptance_criteria>
  <done>Page orchestrates title bar, filter bar, view switcher, and content area. TitleBar shows heading, search, view switcher, CTA. ViewSwitcher persists to localStorage with icon+label segments. FilterBar shows tabbed filters with underline indicator. EmptyState shows contextual icon, heading, description, and optional CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Item, Form, StatusBadge templates</name>
  <files>
    templates/feature/item.tsx.hbs, templates/feature/form.tsx.hbs,
    templates/feature/status-badge.tsx.hbs
  </files>
  <read_first>
    src/features/tasks/components/TaskItem.tsx (gold standard item — note drag handle, status badge, priority flag, actions),
    src/features/tasks/components/TaskForm.tsx (gold standard form — note TanStack Form pattern, Zod validation),
    src/features/tasks/components/TaskStatusBadge.tsx (gold standard badge — note click-to-advance pattern),
    .planning/phases/03.2-crud-generator-upgrade/03.2-UI-SPEC.md (Item Component, Form Component, Status Badge Component sections — read full spec for these)
  </read_first>
  <action>
    **1. `templates/feature/item.tsx.hbs` — List item (per UI spec "Item Component")**

    Container: `group flex items-center gap-4 rounded-lg border border-border bg-card px-4 py-4 shadow-card hover:shadow-card-hover hover:border-border/80 transition-all duration-200 ease-out cursor-pointer`

    Layout (left to right):
    - Drag handle (if `orderable: true`): `p-1 cursor-grab text-primary/30 hover:text-primary/50`, `GripVertical h-4 w-4`
    - Status badge (if any enum field has transitions): `{{pascalCase name}}StatusBadge` component, clickable
    - Title section: `flex-1 min-w-0`
      - Title: `text-sm font-semibold text-primary truncate`
      - Description (if text field exists with showOn list): `text-xs text-primary/50 truncate mt-0.5`
      - Emphasis field (if `views.list.emphasis` configured): `text-xs text-primary/60 truncate mt-0.5`
    - Metadata: `flex items-center gap-4`, each `text-xs text-primary/50`
      - Enum fields as dot-style badges
      - Boolean fields as colored dots
    - Priority flag (if boolean priority field exists): `rounded-md p-1.5 transition-colors hover:bg-primary/5`, active `text-orange-500`, inactive `text-primary/20`, `Flag h-4 w-4`
    - Actions: `flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity duration-150`
      - Delete: `Button ghost size="icon" h-8 w-8 text-destructive/70 hover:text-destructive`, `Trash2 h-4 w-4`, `useDoubleCheck`

    Density support: when `views.list.density` is `compact`, use `py-2 gap-2` instead of `py-4 gap-4`.

    Import `useMutation` from `@tanstack/react-query` and `useConvexMutation` from `@convex-dev/react-query` for mutations.

    **2. `templates/feature/form.tsx.hbs` — Create/edit form (per UI spec "Form Component")**

    Two modes: `inline` (top of page) and `full` (create/edit page or dialog).

    **Inline mode:**
    - Container: `flex items-center gap-2`
    - Input: `flex-1 bg-transparent`, placeholder `"Add a {label}..."`
    - Submit: `Button size="sm"`, `"Add"`
    - Uses `useForm` from `@tanstack/react-form` with `zodValidator` adapter
    - Calls `api.{name}.mutations.create` on submit

    **Full mode:**
    - Container: `flex flex-col gap-6 max-w-xl`
    - Each field: `flex flex-col gap-1.5`
      - Label: `text-xs tracking-wide text-muted-foreground uppercase`
      - Input mapped by field type (using `formInputType` helper):
        - `string` -> `Input` component, `h-10`
        - `text` -> `textarea`, `rounded-md border border-input bg-background px-4 py-2.5 text-sm min-h-[100px] resize-y focus-visible:ring-2 focus-visible:ring-ring`
        - `boolean` -> `Switch` component with inline label
        - `number` -> `Input type="number"` with `tabular-nums`
        - `enum` -> `Select` component with values from schema
      - Validation error: `text-xs text-destructive mt-1`
    - Actions: `flex items-center gap-4 pt-4 border-t border-border`
      - Discard: `Button variant="outline" size="sm"`
      - Submit: `Button size="sm"`, `"Create {Label}"` or `"Save Changes"`

    Import Zod schema for form validation from `@/shared/schemas/{name}`.

    **3. `templates/feature/status-badge.tsx.hbs` — Enum status badge (per UI spec "Status Badge Component")**

    Dot indicator + text pattern (NOT icon-based like existing TaskStatusBadge):
    - Container: `inline-flex items-center gap-1.5 rounded-md px-2.5 py-1.5 text-xs font-semibold transition-colors duration-150 hover:bg-primary/5 disabled:cursor-default disabled:opacity-60`
    - Dot: `span h-2 w-2 rounded-full aria-hidden="true"` with color from enum palette
    - Label: status text
    - Click: advances to next status in transition chain (call `updateStatus` mutation)
    - Disabled at terminal status (no `transitions[currentStatus]` or transitions lead nowhere)
    - Title: `"Mark as {nextLabel}"` or current label at terminal
    - Keyboard: Enter/Space triggers same as click
    - Color classes from `enumBadgeClasses` Handlebars helper

    Color palette mapping (8 slots, round-robin by YAML `values` declaration order):
    - Slot 0: `bg-gray-400` dot, `text-gray-600 dark:text-gray-400` text
    - Slot 1: `bg-blue-500` dot, `text-blue-600 dark:text-blue-400` text
    - Slot 2: `bg-green-500` dot, `text-green-600 dark:text-green-400` text
    - Slot 3: `bg-amber-500` dot, `text-amber-600 dark:text-amber-400` text
    - Slot 4: `bg-red-500` dot, `text-red-600 dark:text-red-400` text
    - Slot 5: `bg-purple-500` dot, `text-purple-600 dark:text-purple-400` text
    - Slot 6: `bg-pink-500` dot, `text-pink-600 dark:text-pink-400` text
    - Slot 7: `bg-cyan-500` dot, `text-cyan-600 dark:text-cyan-400` text

    All templates use `@generated-start`/`@generated-end` and `@custom-start`/`@custom-end` markers.
  </action>
  <verify>
    <automated>grep -q "shadow-card" templates/feature/item.tsx.hbs && grep -q "useForm" templates/feature/form.tsx.hbs && grep -q "rounded-full" templates/feature/status-badge.tsx.hbs && grep -q "group-hover:opacity-100" templates/feature/item.tsx.hbs && echo "All 3 component templates exist with key content"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/feature/item.tsx.hbs` contains `shadow-card`, `hover:shadow-card-hover`, `group-hover:opacity-100`, `GripVertical` (conditional), `useDoubleCheck`, `Trash2`, `text-sm font-semibold text-primary truncate`
    - `templates/feature/form.tsx.hbs` contains `useForm`, `zodValidator`, `api.{{name}}.mutations.create`, `text-xs tracking-wide text-muted-foreground uppercase`, form fields mapped by type (Input, textarea, Switch, Select)
    - `templates/feature/status-badge.tsx.hbs` contains `h-2 w-2 rounded-full`, `gap-1.5`, `hover:bg-primary/5`, `disabled:cursor-default`, transition logic referencing YAML transitions map
    - All 3 templates use `@generated-start` / `@generated-end` markers
    - Item template conditionally includes drag handle, priority flag, assignee based on behaviors
    - Form template generates field inputs based on field type from YAML
  </acceptance_criteria>
  <done>Item template produces premium list items with shadow, hover effects, conditional drag handles/priority/assignee. Form template produces TanStack Form with Zod validation and type-mapped inputs. StatusBadge template produces dot-indicator badges with click-to-advance and color coding.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel export, frontend test template, and i18n locale templates</name>
  <files>
    templates/feature/index.ts.hbs, templates/feature/test.tsx.hbs,
    templates/feature/locale-en.json.hbs, templates/feature/locale-es.json.hbs
  </files>
  <read_first>
    src/features/tasks/index.ts (gold standard barrel export),
    src/features/tasks/tasks.test.tsx (gold standard frontend tests),
    public/locales/en/tasks.json (gold standard i18n file),
    public/locales/es/tasks.json (gold standard Spanish translations),
    .planning/phases/03.2-crud-generator-upgrade/03.2-UI-SPEC.md (Copywriting Contract section),
    .planning/phases/03.2-crud-generator-upgrade/03.2-CONTEXT.md (D-34, D-35 — test patterns)
  </read_first>
  <action>
    **1. `templates/feature/index.ts.hbs` — Barrel export (replaces current stub)**

    ```handlebars
    // @generated-start exports
    export { {{pascalCase name}}Page } from "./components/{{pascalCase name}}Page";
    {{#if views.filteredViews}}
    {{#each views.filteredViews}}
    export { {{pascalCase ../name}}Page as {{pascalCase name}}Page } from "./components/{{pascalCase ../name}}Page";
    {{/each}}
    {{/if}}
    // @generated-end exports
    // @custom-start exports
    // @custom-end exports
    ```

    **2. `templates/feature/test.tsx.hbs` — Frontend test (replaces current stub, per D-34/D-35)**

    Structure matching `src/features/tasks/tasks.test.tsx`:

    ```handlebars
    // @generated-start imports
    import { screen, waitFor } from "@testing-library/react";
    import userEvent from "@testing-library/user-event";
    import { describe, expect } from "vitest";
    import { test } from "@cvx/test.setup";
    import { api } from "~/convex/_generated/api";
    import { renderWithRouter } from "@/test-helpers";
    import { {{pascalCase name}}Page } from "./";
    // @generated-end imports
    // @custom-start imports
    // @custom-end imports
    ```

    Test cases:
    - `"{{pascalCase name}} page renders heading"` — render page, wait for heading text (`t("{labelPlural}")`)
    - `"renders empty state when no {labelPlural}"` — render with no data, verify empty state text
    - `"renders {label} items when data exists"` — create records via `client.mutation`, render, verify items appear
    - `"inline form creates a {label}"` (if operations.create) — fill input, click Add, verify record created
    - `"status badge advances status on click"` (if transitions exist) — click badge, verify status changed
    - `"delete button removes {label}"` (if operations.delete) — click delete (double-check pattern), verify removed

    All test cases use the `test` fixture from `@cvx/test.setup` providing `{ client }`.
    Use `renderWithRouter(<{{pascalCase name}}Page />, client)` pattern.
    Use `userEvent.setup()` for interactions.
    Use `waitFor(() => { expect(...).toBeInTheDocument(); })` for async assertions.

    **3. `templates/feature/locale-en.json.hbs` — English translations (per UI spec Copywriting Contract)**

    ```json
    {
      "nav": {
        "{{name}}": "{{labelPlural}}"
        {{#each views.filteredViews}}
        ,"{{name}}": "{{label}}"
        {{/each}}
      },
      "page": {
        "title": "{{labelPlural}}",
        "description": "Manage your {{lowerCase labelPlural}}."
      },
      "form": {
        "addInline": "Add {{label}}",
        "create": "Create {{label}}",
        "save": "Save Changes",
        "edit": "Edit",
        "discard": "Discard",
        "discardChanges": "Discard Changes",
        "close": "Close"
      },
      "empty": {
        "title": "No {{lowerCase labelPlural}} yet",
        "description": "Create your first {{lowerCase label}} to get started.",
        "cta": "Create {{label}}",
        "noResults": "No results",
        "noResultsDescription": "No {{lowerCase labelPlural}} match the current filter. Try adjusting your filters."
      },
      "error": {
        "notFound": "{{label}} not found.",
        "invalidTransition": "Invalid status transition.",
        "authRequired": "Sign in required.",
        "loadFailedTitle": "Something went wrong",
        "loadFailedDescription": "We couldn't load your {{lowerCase labelPlural}}. Please try again.",
        "retry": "Retry"
      },
      "delete": {
        "confirm": "Delete?"
      },
      "table": {
        "showing": "Showing {{start}}-{{end}} of {{total}}",
        "rowsPerPage": "Rows per page"
      },
      "filter": {
        "all": "All"
      },
      "search": {
        "placeholder": "Search {{lowerCase labelPlural}}..."
      },
      "detail": {
        "editing": "Editing {{recordTitle}}",
        "notSet": "Not set"
      }
    }
    ```

    **4. `templates/feature/locale-es.json.hbs` — Spanish translations**

    Same structure with Spanish translations. Use standard Spanish for CRUD terms:
    - "Add" -> "Agregar", "Create" -> "Crear", "Save Changes" -> "Guardar cambios"
    - "Edit" -> "Editar", "Delete?" -> "Eliminar?", "Discard" -> "Descartar"
    - "No {{labelPlural}} yet" -> "Aun no hay {{labelPlural}}"
    - "Something went wrong" -> "Algo salio mal"
    - etc.
  </action>
  <verify>
    <automated>grep -q "renderWithRouter" templates/feature/test.tsx.hbs && grep -q "userEvent" templates/feature/test.tsx.hbs && grep -q "pascalCase name" templates/feature/index.ts.hbs && ls templates/feature/locale-en.json.hbs templates/feature/locale-es.json.hbs && echo "All 4 templates exist with key content"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/feature/index.ts.hbs` contains `export { {{pascalCase name}}Page }`, `@generated-start exports`, `@custom-start exports`
    - `templates/feature/test.tsx.hbs` contains `import { test } from "@cvx/test.setup"`, `renderWithRouter`, `userEvent.setup()`, `waitFor`, at least 4 test case templates
    - `templates/feature/locale-en.json.hbs` contains `nav`, `page`, `form`, `empty`, `error`, `delete`, `table`, `filter`, `search`, `detail` sections
    - `templates/feature/locale-es.json.hbs` contains Spanish translations matching the English structure
    - Frontend test template tests rendering, empty state, data display, form submission, status advancement, and deletion
  </acceptance_criteria>
  <done>Barrel export re-exports all page components. Frontend test template produces comprehensive tests with renderWithRouter and userEvent. i18n locale templates produce English and Spanish translation files with all UI copy.</done>
</task>

</tasks>

<verification>
- All 10 template files exist in `templates/feature/`
- Templates use premium CSS classes from UI spec (shadow-card, hover:shadow-card-hover, text-primary/60, etc.)
- Templates reference design system tokens (surface-secondary, Inter Variable via font stack)
- Templates include `@generated-start`/`@generated-end` and `@custom-start`/`@custom-end` markers
- Form template maps field types to correct input components
- Test template follows gold standard pattern
</verification>

<success_criteria>
1. Page template orchestrates title bar, filters, view switching, and content rendering
2. Item template produces premium list items with shadows, hover effects, and conditional behaviors
3. Form template produces TanStack Form with Zod validation and type-mapped inputs
4. StatusBadge template produces dot-indicator badges with click-to-advance
5. Frontend test template produces tests matching the tasks gold standard
6. i18n templates produce complete English and Spanish translations
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-crud-generator-upgrade/03.2-03-SUMMARY.md`
</output>
