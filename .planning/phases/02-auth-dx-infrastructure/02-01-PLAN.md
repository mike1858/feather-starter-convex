---
phase: 02-auth-dx-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/auth.ts
  - convex/password/ResendOTPPasswordReset.ts
  - convex/password/PasswordResetEmail.tsx
  - src/features/auth/components/PasswordForm.tsx
  - src/features/auth/components/PasswordForm.test.tsx
  - src/routes/_app/login/_layout.index.tsx
  - vitest.config.ts
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "User can sign up with email and password"
    - "User can sign in with email and password"
    - "OTP and GitHub OAuth continue to work alongside password auth"
  artifacts:
    - path: "convex/auth.ts"
      provides: "Password provider registration alongside existing OTP and GitHub"
      contains: "Password"
    - path: "convex/password/ResendOTPPasswordReset.ts"
      provides: "Password reset email provider (stub — wired in Plan 03)"
      exports: ["ResendOTPPasswordReset"]
    - path: "src/features/auth/components/PasswordForm.tsx"
      provides: "Email/password sign-up and sign-in form"
      exports: ["PasswordForm"]
    - path: "src/routes/_app/login/_layout.index.tsx"
      provides: "Login page with password form alongside OTP and GitHub"
  key_links:
    - from: "src/features/auth/components/PasswordForm.tsx"
      to: "@convex-dev/auth/react"
      via: "useAuthActions().signIn('password', { flow })"
      pattern: "signIn.*password.*flow"
    - from: "convex/auth.ts"
      to: "@convex-dev/auth/providers/Password"
      via: "Password provider import"
      pattern: "Password"
    - from: "src/routes/_app/login/_layout.index.tsx"
      to: "src/features/auth/components/PasswordForm.tsx"
      via: "PasswordForm component import"
      pattern: "PasswordForm"
---

<objective>
Add email/password authentication as a third auth method alongside existing OTP and GitHub OAuth.

Purpose: Users can sign up and sign in with email + password (AUTH-01), the most common auth flow for SaaS apps.
Output: Password provider registered in Convex Auth, PasswordForm component, updated login page with all three auth methods.
</objective>

<execution_context>
@/Users/siraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/siraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-dx-infrastructure/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From convex/auth.ts (current — will be modified):
```typescript
import { convexAuth } from "@convex-dev/auth/server";
import GitHub from "@auth/core/providers/github";
import { ResendOTP } from "./otp/ResendOTP";

export const { auth, signIn, signOut, store } = convexAuth({
  providers: [ResendOTP, GitHub({ authorization: { params: { scope: "user:email" } } })],
});
```

From convex/otp/ResendOTP.ts (pattern to follow for reset provider):
```typescript
import { Email } from "@convex-dev/auth/providers/Email";
import { alphabet, generateRandomString } from "oslo/crypto";
import { Resend as ResendAPI } from "resend";
import { AUTH_EMAIL, AUTH_RESEND_KEY } from "@cvx/env";

export const ResendOTP = Email({
  id: "resend-otp",
  apiKey: AUTH_RESEND_KEY,
  maxAge: 60 * 20,
  async generateVerificationToken() {
    return generateRandomString(8, alphabet("0-9"));
  },
  async sendVerificationRequest({ identifier: email, provider, token, expires }) { ... },
});
```

From src/routes/_app/login/_layout.index.tsx (login page structure):
- Uses `useAuthActions()` from `@convex-dev/auth/react`
- Two-step OTP flow: LoginForm -> VerifyForm
- GitHub OAuth button below divider
- Styling: max-w-96, centered, gap-6 layout
- Form handling: `@tanstack/react-form` + Zod validators
- After auth: redirects to onboarding (no username) or dashboard

From vitest.config.ts (coverage excludes):
- `convex/otp/**` is excluded from coverage
- `convex/auth.ts` is excluded from coverage
- `src/features/**/index.ts` barrel files excluded
- `src/routes/**` excluded
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Password provider backend + reset provider stub</name>
  <files>convex/auth.ts, convex/password/ResendOTPPasswordReset.ts, convex/password/PasswordResetEmail.tsx, vitest.config.ts</files>
  <behavior>
    - Password provider is registered in convexAuth alongside ResendOTP and GitHub
    - ResendOTPPasswordReset provider exists with unique id "password-reset" (not "resend-otp" to avoid collision with OTP provider)
    - PasswordResetEmail renders a React Email template with a reset code
  </behavior>
  <action>
    1. Create `convex/password/ResendOTPPasswordReset.ts`:
       - Use `Resend` from `@auth/core/providers/resend` (same pattern as ResendOTP but for password reset)
       - Set `id: "password-reset"` (CRITICAL: must NOT be "resend-otp" — see Pitfall 1 in RESEARCH.md)
       - Import `AUTH_RESEND_KEY`, `AUTH_EMAIL` from `@cvx/env`
       - Use `@oslojs/crypto/random` for token generation: `import { generateRandomString, RandomReader } from "@oslojs/crypto/random"` with alphabet "0123456789" and length 8
       - In `sendVerificationRequest`, use Resend API to send email with `PasswordResetEmail` React template
       - Export as `ResendOTPPasswordReset`

    2. Create `convex/password/PasswordResetEmail.tsx`:
       - Follow pattern of `convex/otp/VerificationCodeEmail.tsx`
       - Use `@react-email/components` for layout
       - Display the reset code prominently
       - Include "Reset your password" messaging

    3. Update `convex/auth.ts`:
       - Import `Password` from `@convex-dev/auth/providers/Password`
       - Import `ResendOTPPasswordReset` from `./password/ResendOTPPasswordReset`
       - Add `Password({ reset: ResendOTPPasswordReset })` to providers array

    4. Update `vitest.config.ts` coverage excludes:
       - Add `"convex/password/**"` to the exclude array (same pattern as `convex/otp/**` — auth provider config, not unit-testable logic)
  </action>
  <verify>
    <automated>npx vitest run --coverage</automated>
  </verify>
  <done>Password provider registered in convex/auth.ts with reset capability. Coverage still passes at 100% with new exclusions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PasswordForm component + login page integration</name>
  <files>src/features/auth/components/PasswordForm.tsx, src/features/auth/components/PasswordForm.test.tsx, src/routes/_app/login/_layout.index.tsx</files>
  <behavior>
    - PasswordForm renders email and password fields with a submit button
    - PasswordForm has signIn/signUp mode toggle
    - PasswordForm calls signIn("password", { email, password, flow: "signIn"|"signUp" })
    - Login page shows PasswordForm as the primary form, with OTP and GitHub below as alternatives
  </behavior>
  <action>
    1. Create `src/features/auth/components/PasswordForm.tsx`:
       - Use `useAuthActions()` from `@convex-dev/auth/react`
       - Use `@tanstack/react-form` with Zod validators (same pattern as existing LoginForm)
       - Two fields: email (z.string().email()) and password (z.string().min(8))
       - State for mode: `signIn` | `signUp`, with toggle button
       - On submit: `signIn("password", { email, password, flow: mode })`
       - Include a "Forgot password?" link/button that calls `onForgotPassword` prop (wired in Plan 03)
       - Show loading spinner during submission (Loader2 from lucide-react)
       - Use same styling patterns as existing LoginForm (bg-transparent inputs, w-full buttons)
       - Accept `onSuccess` callback prop for navigation after auth

    2. Create `src/features/auth/components/PasswordForm.test.tsx`:
       - Test that form renders email and password fields
       - Test mode toggle between signIn and signUp
       - Test form validation (email format, password min length)
       - Mock `useAuthActions` to verify `signIn` called with correct params

    3. Update `src/routes/_app/login/_layout.index.tsx`:
       - Import `PasswordForm` from `@/features/auth/components/PasswordForm`
       - Replace the current two-step OTP-first flow with a tabbed/sectioned layout:
         - Primary: PasswordForm (email + password)
         - Divider: "Or continue with"
         - Secondary: "Continue with Email (OTP)" button that enters the existing OTP flow
         - Secondary: GitHub button (existing)
       - Keep the existing OTP VerifyForm for when OTP flow is active
       - Keep the existing redirect logic (onboarding vs dashboard) in the parent Login component
  </action>
  <verify>
    <automated>npx vitest run --coverage</automated>
  </verify>
  <done>PasswordForm component with tests. Login page shows email/password as primary auth method with OTP and GitHub as alternatives. All tests pass with 100% coverage.</done>
</task>

</tasks>

<verification>
- `npx vitest run --coverage` passes with 100% thresholds
- `convex/auth.ts` has Password provider with reset option
- Login page renders email/password form with mode toggle
- OTP and GitHub auth paths still accessible from login page
</verification>

<success_criteria>
- User can see email + password form on login page
- User can toggle between sign-in and sign-up modes
- Password provider is registered in Convex Auth config
- OTP and GitHub buttons remain accessible
- All unit tests pass at 100% coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-dx-infrastructure/02-01-SUMMARY.md`
</output>
