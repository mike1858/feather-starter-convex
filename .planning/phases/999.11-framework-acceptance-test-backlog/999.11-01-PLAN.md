# Phase 999.11 Plan 1: Offline pipeline hardening — typecheck + build verification

**Status:** PENDING
**Phase:** 999.11 — Framework Acceptance Test — Full User Journey E2E
**Scope:** Add build verification tests that run `npm run typecheck` and `npm run build` against the current project, proving all generated code compiles and builds correctly
**Success Criteria:** SC-1
**Dependencies:** None (runs independently)
**Estimated Tasks:** 2

## Context

The existing integration test (`scripts/create/integration.test.ts`) validates the strip/scaffold/add round-trip via file existence checks only — no typecheck, no build. Running `tsc` or `vite build` on a temp scaffold is impractical (needs `npm install`). Instead, we verify the CURRENT project (which has all features installed) typechecks and builds.

**Key files:**
- `scripts/create/integration.test.ts` — existing pipeline test (file existence only)
- `package.json` — `typecheck` runs 3 tsconfigs, `build` runs tsc + vite build
- `tsconfig.app.json`, `tsconfig.node.json`, `convex/tsconfig.json` — all 3 must pass

**Pattern:** Shell out to `npm run typecheck` and `npm run build` from a Vitest test using `execSync`, assert exit code 0.

## Tasks

- [ ] **Task 1.1:** Create build verification test file

  Create `scripts/create/build-verification.test.ts` with two test cases:

  1. **"typecheck passes for all tsconfigs"** — Run `npm run typecheck` via `execSync` from the project root (`process.cwd()`). Assert it completes without throwing. Wrap in try/catch to capture stderr on failure for a clear error message.

  2. **"vite build succeeds"** — Run `npm run build` via `execSync` from the project root. Assert it completes without throwing. After the test, clean up the `dist/` directory if it was created (use `afterAll` with `fs.rmSync("dist", { recursive: true, force: true })`).

  Both tests should set a generous timeout (60 seconds) since `tsc` and `vite build` can be slow. Use `{ encoding: "utf-8", timeout: 60_000 }` options on `execSync`.

  Import pattern:
  ```typescript
  import { describe, test, expect, afterAll } from "vitest";
  import { execSync } from "node:child_process";
  import { rmSync } from "node:fs";
  ```

  **Files:** `scripts/create/build-verification.test.ts`
  **Verify:** `npx vitest run scripts/create/build-verification.test.ts` passes with both tests green

- [ ] **Task 1.2:** Run full verification and clean up build artifacts

  Run the new test file to confirm both typecheck and build pass. Then run the existing integration test suite to ensure no regressions: `npx vitest run scripts/create/`. Both test files should pass.

  Verify that `dist/` is cleaned up after the build test (the `afterAll` hook in Task 1.1 handles this).

  If the build test creates artifacts that interfere with the dev server or git, add `dist/` to `.gitignore` if not already present.

  **Files:** `scripts/create/build-verification.test.ts` (verify only), `.gitignore` (if needed)
  **Verify:** `npx vitest run scripts/create/` passes — all existing + new tests green, no leftover `dist/` directory
