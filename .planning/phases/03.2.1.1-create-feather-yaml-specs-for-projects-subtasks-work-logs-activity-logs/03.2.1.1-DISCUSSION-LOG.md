# Phase 03.2.1.1: Create feather.yaml specs — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-29
**Phase:** 03.2.1.1-create-feather-yaml-specs
**Areas discussed:** Spec completeness standard, Reality vs aspiration, Old gen.yaml cleanup, Activity logs special case

---

## Spec Completeness Standard

| Option | Description | Selected |
|--------|-------------|----------|
| Full parity with tasks | Every spec gets all applicable sections: identity, hooks, detailView, derivedData | ✓ |
| Match current implementation | Only spec what's already built | |
| Progressive — required sections only | Only sections the generator currently consumes | |

**User's choice:** Full parity with tasks
**Notes:** User confirmed all sections should be present even if generator doesn't consume them yet.

### Follow-up: Aspirational Section Markers

| Option | Description | Selected |
|--------|-------------|----------|
| Commit as-is, no markers | The spec IS the contract, no noise | |
| Mark with # TODO: generator pending | Explicit about what's aspirational | ✓ |

**User's choice:** Mark with # TODO: generator pending
**Notes:** Prevents confusion about what generator currently handles.

### Follow-up: Tasks Reconciliation Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Only the 4 entities | Phase scope as stated | |
| Include tasks reconciliation | Reconcile tasks.gen.yaml into feather.yaml too | ✓ |

**User's choice:** Include tasks reconciliation
**Notes:** User asked about relationship between gen.yaml and feather.yaml, and why tasks has both. After explanation of the format evolution (gen.yaml = generator input, feather.yaml = feature contract), user decided to include tasks in scope.

---

## Reality vs Aspiration

| Option | Description | Selected |
|--------|-------------|----------|
| Current implementation as baseline + aspirational sections marked | Fields/access/indexes reflect today, aspirational sections marked | |
| Desired end state only | Spec the ideal, gaps are generator's backlog | ✓ |

**User's choice:** Desired end state only
**Notes:** Specs become the contract for what the system *should* be.

### Follow-up: Spec Ideal Behaviors

| Option | Description | Selected |
|--------|-------------|----------|
| Spec ideal behaviors too | If subtasks should have promotion, spec it | ✓ |
| Only structural completeness | Add sections but don't invent new behaviors | |

**User's choice:** Spec ideal behaviors too
**Notes:** Subtask promotion hooks, project status transitions, etc. all get specced even if not yet built.

---

## Old gen.yaml Cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Delete after merging into feather.yaml | Diff, merge, delete — single source of truth | ✓ |
| Keep both, mark gen.yaml as deprecated | Less risk but two files confusing | |
| Delete immediately, no merge step | Assume feather.yaml already has everything | |

**User's choice:** Delete after merging into feather.yaml
**Notes:** User first asked for clarification on feather.yaml design (per-feature vs bundles, why rename from gen.yaml, custom code handling). After explanation referencing 999.1-CONTEXT.md decisions, user confirmed merge-then-delete approach.

### Follow-up: Example Apps Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Out of scope — only 5 CalmDo entities | Defer todos/tickets/contacts to 999.4 | |
| Include them | All 8 entities get feather.yaml | ✓ |

**User's choice:** Include them
**Notes:** Scope expanded from 5 to 8 entities.

---

## Activity Logs Special Case

| Option | Description | Selected |
|--------|-------------|----------|
| Feature YAML with system-entity markers | kind: system flag, generator skips CRUD | ✓ |
| Separate schema-only spec | Only define schema + indexes + views | |
| Inline in parent entities | No own feather.yaml, define in parent YAMLs | |

**User's choice:** Feature YAML with system-entity markers
**Notes:** Activity logs keep their own feather.yaml with `kind: system`.

### Follow-up: Actor Field Type

| Option | Description | Selected |
|--------|-------------|----------|
| belongs_to users with column: actorId | Typed relationship, generator produces joined queries | ✓ |
| Keep as plain string | Store userId as string, resolve separately | |

**User's choice:** belongs_to users with column: actorId
**Notes:** Consistent with assigneeId pattern in tasks.

---

## Claude's Discretion

- Field-level details for example apps (todos, tickets, contacts) — infer from existing gen.yaml content
- Index optimization — add indexes appropriate for query patterns
- `kind: system` vs `systemGenerated: true` naming in YAML

## Deferred Ideas

- Generator upgrade to read feather.yaml — Phase 999.4
- Bundle manifest format — Phase 999.6
