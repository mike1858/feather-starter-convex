---
phase: 999.5-excel-to-system-pipeline-backlog
verified: 2026-04-01T10:58:00Z
status: gaps_found
score: 5/8 must-haves verified
gaps:
  - truth: "Dependencies are installed and test suite passes"
    status: failed
    reason: "xlsx, @anthropic-ai/sdk, and @clack/prompts are declared in package.json but not installed in node_modules. This causes 3 test files to fail at the import-resolution stage, plus 2 additional test failures for unexported helper functions."
    artifacts:
      - path: "node_modules/xlsx"
        issue: "Not installed — npm install not run after package.json was updated"
      - path: "node_modules/@anthropic-ai/sdk"
        issue: "Not installed — causes convex/imports/actions.test.ts to fail with ERR_MODULE_NOT_FOUND"
      - path: "node_modules/@clack/prompts"
        issue: "Not installed — causes bin/__tests__/import.test.ts and CLI to fail"
      - path: "templates/pipeline/excel/type-inference.ts"
        issue: "toFieldName and toEntityName are private functions (no export) but type-inference.test.ts imports and tests them directly — 3 test failures"
    missing:
      - "Run npm install to install xlsx, @anthropic-ai/sdk, @clack/prompts"
      - "Export toFieldName and toEntityName from type-inference.ts, or remove their direct-import tests and test only via inferEntities/inferFieldType"

  - truth: "Import history (Imports) appears in sidebar navigation"
    status: failed
    reason: "src/shared/nav.ts has 'Import' entry (wizard) but is missing the 'Imports' entry for import history at /dashboard/imports. Plan 05 explicitly required this nav entry."
    artifacts:
      - path: "src/shared/nav.ts"
        issue: "Only 'Import' at /dashboard/import is present. 'Imports' at /dashboard/imports is absent."
    missing:
      - "Add { label: 'Imports', i18nKey: 'import.nav.imports', to: '/dashboard/imports' } to navItems array in src/shared/nav.ts"

  - truth: "All components are exported from the feature barrel"
    status: failed
    reason: "ImportHistory, ImportReport, ReconciliationDiff, and useReconciliation are not exported from src/features/import/index.ts despite being required by routes and tests."
    artifacts:
      - path: "src/features/import/index.ts"
        issue: "Missing exports: ImportHistory, ImportReport, ReconciliationDiff, useReconciliation"
    missing:
      - "Add export { ImportHistory } from './components/ImportHistory'"
      - "Add export { ImportReport } from './components/ImportReport'"
      - "Add export { ReconciliationDiff } from './components/ReconciliationDiff'"
      - "Add export { useReconciliation } from './hooks/useReconciliation'"
---

# Phase 999.5: Excel-to-System Pipeline Verification Report

**Phase Goal:** User uploads Excel file, system infers schema, user confirms, system produces feather.yaml + seed data
**Verified:** 2026-04-01T10:58:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User can upload Excel file via drag-and-drop or file input | VERIFIED | `DropZone.tsx` (98 lines) handles both interactions, validates .xlsx/.xls, shows spinner |
| 2 | System infers schema (entities, fields, types, relationships) from Excel | VERIFIED | `type-inference.ts` + `entity-classifier.ts` + `analyzeExcel` Convex action with LLM+heuristic fallback |
| 3 | User can confirm schema through 3-step wizard + dashboard review | VERIFIED | `ImportWizard.tsx`, `Step1Entities.tsx`, `Step2Fields.tsx`, `Step3Relationships.tsx`, `DashboardReview.tsx` all exist and are wired |
| 4 | System produces feather.yaml files from confirmed schema | VERIFIED | `yaml-generator.ts` (174 lines) with `generateFeatherYaml`/`generateAllYamls`; CLI writes to disk; web mode computes but cannot write (browser limitation, acceptable) |
| 5 | System imports Excel data into Convex with three-tier error handling | VERIFIED | `importData` Convex action (344 lines) processes rows in dependency order with green/yellow/red error classification |
| 6 | Re-import reconciliation detects column renames/additions/removals | VERIFIED | `reconciliation.ts` (224 lines) with Jaro-Winkler fuzzy matching, `detectReimport` and `reconcileSheet` |
| 7 | Dependencies are installed and test suite passes with 100% coverage | FAILED | `xlsx`, `@anthropic-ai/sdk`, `@clack/prompts` not in node_modules; 4 test files failing (5 test failures total including `toFieldName`/`toEntityName` not exported) |
| 8 | Import history visible in sidebar nav and accessible via /dashboard/imports | FAILED | Route exists, component exists, but "Imports" nav entry missing from `src/shared/nav.ts` |

**Score:** 5/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `templates/pipeline/excel/parser.ts` | SheetJS-based Excel parser | VERIFIED | 152 lines, exports parseExcelWorkbook, SheetMetadata, ColumnInfo |
| `templates/pipeline/excel/type-inference.ts` | Heuristic field/entity type inference | VERIFIED | 215 lines, exports inferFieldType, inferEntityType, inferEntities |
| `templates/pipeline/excel/jaro-winkler.ts` | Weighted column similarity | VERIFIED | 154 lines, exports jaroWinklerSimilarity, computeColumnMatchScore |
| `templates/pipeline/excel/entity-classifier.ts` | FK detection + topological sort | VERIFIED | 163 lines, exports detectRelationships, classifyWorkbook, computeImportOrder |
| `templates/pipeline/excel/yaml-generator.ts` | feather.yaml from confirmed schema | VERIFIED | 174 lines, exports generateFeatherYaml, generateAllYamls |
| `templates/pipeline/excel/data-importer.ts` | Three-tier error coercion | VERIFIED | 260 lines, exports coerceValue, processRow with green/yellow/red classification |
| `templates/pipeline/excel/reconciliation.ts` | Re-import reconciliation engine | VERIFIED | 224 lines, exports reconcileSheet, detectReimport |
| `templates/pipeline/excel/index.ts` | Barrel export for pipeline | VERIFIED | All 7 modules exported (parser, type-inference, jaro-winkler, entity-classifier, yaml-generator, data-importer, reconciliation) |
| `convex/schema.ts` | imports, schemaMappings, importErrors tables | VERIFIED | All three tables present at lines 128, 162, 177 |
| `convex/imports/mutations.ts` | CRUD mutations for import workflow | VERIFIED | 156 lines |
| `convex/imports/queries.ts` | list, get, getErrors queries | VERIFIED | 54 lines |
| `convex/imports/actions.ts` | analyzeExcel + importData Convex actions | VERIFIED | 344 lines, both actions exported and substantive |
| `convex/schemaMappings/mutations.ts` | Schema mapping CRUD | VERIFIED | 80 lines |
| `convex/schemaMappings/queries.ts` | Schema mapping queries | VERIFIED | 46 lines |
| `src/features/import/components/DropZone.tsx` | Drag-and-drop upload | VERIFIED | 98 lines, validates .xlsx/.xls, loading spinner |
| `src/features/import/components/ImportWizard.tsx` | 5-step wizard container | VERIFIED | 163 lines, wired to all steps + confirm handler |
| `src/features/import/components/Step1Entities.tsx` | Entity review with confidence | VERIFIED | 59 lines, editable labels, entity type badge, confidence colors |
| `src/features/import/components/Step2Fields.tsx` | Field type/required editing | VERIFIED | 138 lines, FieldTypeDropdown, ColumnGroupAccordion for 30+ fields |
| `src/features/import/components/Step3Relationships.tsx` | Relationship review | VERIFIED | 210 lines, remove/add relationships |
| `src/features/import/components/DashboardReview.tsx` | Final review + confirm button | VERIFIED | 149 lines, tabbed entities, inline editing, "Confirm & Generate" wired to confirmSchema + generateAllYamls + navigate |
| `src/features/import/components/FieldTypeDropdown.tsx` | Type correction dropdown | VERIFIED | 35 lines, 11 field types |
| `src/features/import/components/SampleDataPreview.tsx` | Sample data display | VERIFIED | 58 lines, compact and full modes |
| `src/features/import/components/ColumnGroupAccordion.tsx` | Semantic grouping for 30+ cols | VERIFIED | 199 lines |
| `src/features/import/components/ReconciliationDiff.tsx` | Flat + git-style diff toggle | VERIFIED | 332 lines, flat list and side-by-side diff views |
| `src/features/import/components/ImportReport.tsx` | Three-tier error report | VERIFIED | 228 lines, green/yellow/red sections per entity |
| `src/features/import/components/ImportHistory.tsx` | Import history list | VERIFIED | 85 lines, status badges, stats, links to report |
| `src/features/import/hooks/useImportWizard.ts` | Wizard state machine | VERIFIED | 160 lines, step navigation, updateEntity/updateField |
| `src/features/import/hooks/useSchemaAnalysis.ts` | Client-side analysis hook | VERIFIED | 137 lines, client-side parse + heuristic result immediately |
| `src/features/import/hooks/useReconciliation.ts` | Re-import detection hook | VERIFIED | 56 lines |
| `src/features/import/index.ts` | Feature barrel export | STUB | Missing: ImportHistory, ImportReport, ReconciliationDiff, useReconciliation |
| `src/routes/_app/_auth/dashboard/_layout.import.tsx` | Import wizard route | VERIFIED | Imports ImportWizard from @/features/import |
| `src/routes/_app/_auth/dashboard/_layout.imports.tsx` | Import history route | VERIFIED | Imports ImportHistory from component directly |
| `src/routes/_app/_auth/dashboard/imports.$importId.tsx` | Import detail route | VERIFIED | Queries imports.get + imports.getErrors, renders ImportReport |
| `bin/commands/import.ts` | CLI import command | VERIFIED | 191 lines, Commander.js with --generate, --output, --no-confirm |
| `bin/feather.ts` | CLI registration | VERIFIED | importCommand registered at line 26 |
| `src/shared/nav.ts` | Nav entries for Import + Imports | PARTIAL | "Import" (wizard) present; "Imports" (history) absent |
| `node_modules/xlsx` | SheetJS installed | MISSING | Declared in package.json but not installed |
| `node_modules/@anthropic-ai/sdk` | Anthropic SDK installed | MISSING | Declared in package.json but not installed |
| `node_modules/@clack/prompts` | CLI prompts library | MISSING | Declared in package.json but not installed |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| DropZone | useSchemaAnalysis.analyzeFile | onFileSelected prop → handleFileSelected | WIRED | ImportWizard.handleFileSelected calls analyzeFile, passes result to wizard.setAnalysisResult |
| useSchemaAnalysis | convex/imports/mutations.create | createImport useMutation | WIRED | useSchemaAnalysis.ts:213 calls createImport |
| DashboardReview "Confirm & Generate" | convex/imports/mutations.confirmSchema | onConfirm → handleConfirm | WIRED | ImportWizard:54 calls confirmSchema with importId + JSON schema |
| DashboardReview confirm | yaml-generator.generateAllYamls | handleConfirm:61 | WIRED (web-no-write) | Called but result discarded in web mode — by design (browser can't write to FS; CLI handles writes) |
| DashboardReview confirm | navigate to /dashboard/imports/{id} | useNavigate | WIRED | ImportWizard:67 navigates to report page |
| analyzeExcel action | Anthropic SDK | process.env.ANTHROPIC_API_KEY check | WIRED | Falls back to heuristics when key absent |
| analyzeExcel action | imports/mutations.saveAnalysis | ctx.runMutation(api.imports.mutations.saveAnalysis) | WIRED | Line 68 of actions.ts |
| importData action | data-importer.processRow | processEntityRows helper | WIRED | Actions.ts:253 calls processEntityRows which calls processRow |
| importData action | imports/mutations.saveErrors | ctx.runMutation(api.imports.mutations.saveErrors) | WIRED | Actions.ts:268 in BATCH_SIZE loop |
| /dashboard/imports/$importId route | imports.queries.get + imports.queries.getErrors | useQuery | WIRED | imports.$importId.tsx:15-21 |
| bin/feather.ts | bin/commands/import.ts | program.addCommand(importCommand) | WIRED | bin/feather.ts:26 |
| CLI import command | yaml-generator.generateAllYamls | generateAllYamls call | WIRED | import.ts writes YAML files to filesystem |
| src/shared/nav.ts | /dashboard/imports | "Imports" nav entry | NOT_WIRED | Entry missing — history route exists but no sidebar link |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `ImportWizard` | `wizard.state.entities` | `useSchemaAnalysis.analyzeFile` → `classifyWorkbook` | Yes — heuristic/LLM from parsed Excel | FLOWING |
| `ImportHistory` | `imports` | `useQuery(api.imports.queries.list)` | Yes — queries imports table by userId | FLOWING |
| `ImportReport` | `importDoc`, `errors` | `useQuery(api.imports.queries.get/getErrors)` | Yes — queries imports + importErrors tables | FLOWING |
| `DashboardReview` | `entities`, `relationships` | Props from wizard state | Yes — populated from analyzeFile | FLOWING |
| `analyzeExcel action` | `result` (AnalysisResult) | `analyzeWithHeuristics` or `analyzeWithLLM` | Yes — parses real Excel file from storage | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| CLI import --help | `npx tsx bin/feather.ts import --help` | ERR_MODULE_NOT_FOUND for @clack/prompts | FAIL — deps not installed |
| xlsx package importable | `node -e "require('xlsx')"` | Module not found | FAIL |
| @anthropic-ai/sdk importable | `node -e "require('@anthropic-ai/sdk')"` | Module not found | FAIL |
| Test suite | `npm test` | 4 files failed / 5 tests failed out of 1037 | FAIL |
| Core pipeline (no deps) | `npm test -- templates/pipeline/excel/entity-classifier --silent` | PASSES | PASS |
| Core pipeline (no deps) | `npm test -- templates/pipeline/excel/yaml-generator --silent` | PASSES | PASS |
| Core pipeline (no deps) | `npm test -- templates/pipeline/excel/reconciliation --silent` | PASSES | PASS |
| Core pipeline (no deps) | `npm test -- templates/pipeline/excel/data-importer --silent` | PASSES | PASS |

### Requirements Coverage

No formal requirement IDs assigned to this backlog phase. Coverage assessed against phase design decisions (D-01 through D-15):

| Design Decision | Status | Evidence |
|----------------|--------|----------|
| D-01: Web upload + CLI `feather import` | SATISFIED | DropZone + ImportWizard + bin/commands/import.ts |
| D-03: 3-step wizard (entities/fields/relationships) | SATISFIED | Step1Entities, Step2Fields, Step3Relationships + step indicator |
| D-04: Dashboard review after wizard | SATISFIED | DashboardReview with tabbed entity layout |
| D-05: Inline type correction dropdowns | SATISFIED | FieldTypeDropdown in Step2Fields + DashboardReview |
| D-06: Sample data always shown | SATISFIED | SampleDataPreview in Step2Fields |
| D-07: Semantic column grouping for 30+ cols | SATISFIED | ColumnGroupAccordion (199 lines) |
| D-09: Auto-detect re-import from content | SATISFIED | reconciliation.ts + useReconciliation hook |
| D-10: Reconciliation diff (flat + git-style toggle) | SATISFIED | ReconciliationDiff (332 lines) with viewMode state |
| D-11: Removed column handling (archive/delete/keep) | SATISFIED | ReconciliationDiff action dropdowns for removals |
| D-12: Import history as first-class entity with permalink | SATISFIED | ImportHistory + /dashboard/imports route + /imports/{id} route |
| D-13: Auto-import after schema confirmation | SATISFIED | importData Convex action wired to confirm flow |
| D-14: Three-tier error handling (green/yellow/red) | SATISFIED | data-importer.coerceValue + importData action + ImportReport |
| D-15: All tiers visible in import report | SATISFIED | ImportReport groups errors by severity |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `templates/pipeline/excel/type-inference.ts` | 197, 205 | `toFieldName`, `toEntityName` not exported but tested directly | Warning | 3 test failures for `toFieldName`/`toEntityName` describe blocks |
| `src/features/import/index.ts` | — | ImportHistory, ImportReport, ReconciliationDiff, useReconciliation missing from barrel | Warning | Routes import from component paths directly, bypassing the barrel |
| `src/shared/nav.ts` | — | "Imports" history nav entry absent | Blocker | Import history at /dashboard/imports is unreachable from sidebar |
| `package.json` | 32, 34, 72 | Deps declared but not installed (`npm install` not run) | Blocker | 4 test files fail; CLI command and Convex actions non-functional |

### Human Verification Required

#### 1. Web UI Upload Flow

**Test:** Navigate to `/dashboard/import`, drag an Excel file with multiple sheets onto the drop zone.
**Expected:** Spinner appears, then Step 1 shows entity cards with confidence percentages, entity type badges, and field counts.
**Why human:** Requires a running Convex dev server + real Excel file; can't verify drag-and-drop behavior programmatically.

#### 2. Confirm & Generate End-to-End

**Test:** Complete the full wizard (upload → entities → fields → relationships → review), click "Confirm & Generate".
**Expected:** Schema saved, page navigates to `/dashboard/imports/{id}` showing the import report with green/yellow/red error sections.
**Why human:** Requires Convex backend + file storage integration; multi-step async flow.

#### 3. YAML Output Verification (Web Mode)

**Test:** After clicking "Confirm & Generate", inspect console or add a download link to verify the `generateAllYamls` call produces valid feather.yaml content.
**Expected:** YAML content matches feather.yaml schema (name, label, fields, timestamps, identity, access, operations, behaviors, views).
**Why human:** In web mode the YAML is computed but not written; needs UX decision on whether to surface the YAML output to the user (download link, copy-to-clipboard, or show in a code block).

### Gaps Summary

Three gaps block full goal achievement:

**Gap 1 — Dependencies not installed (Blocker):** `xlsx`, `@anthropic-ai/sdk`, and `@clack/prompts` are listed in `package.json` but `npm install` was not run after the phase completed. This causes 4 test files to fail at import resolution and makes the CLI command non-executable. Fix: `npm install`. This is a one-command fix.

**Gap 2 — "Imports" nav entry missing (Blocker):** The import history route exists at `/dashboard/imports` and is fully implemented, but has no sidebar nav entry. Users cannot discover the history page from the UI. Fix: add one nav item to `src/shared/nav.ts`.

**Gap 3 — `toFieldName`/`toEntityName` not exported (Warning):** These private helper functions are tested directly in `type-inference.test.ts`. The tests fail because the functions lack `export`. Either export them or test them indirectly via `inferEntities`. 3 test failures.

**Gap 4 — Barrel export incomplete (Warning):** `ImportHistory`, `ImportReport`, `ReconciliationDiff`, `useReconciliation` exist in the feature directory but are absent from `src/features/import/index.ts`. Routes currently import directly from component paths, which works, but violates the feature barrel pattern.

The core pipeline is architecturally sound and functionally complete. All pipeline modules (parser, type-inference, entity-classifier, yaml-generator, data-importer, reconciliation), all UI components (wizard, steps, review, report, history), all Convex tables (imports, schemaMappings, importErrors) and mutations/queries/actions exist and are substantive. The gaps are operational (missing npm install), navigational (missing nav entry), and organizational (missing exports) — not architectural.

---

_Verified: 2026-04-01T10:58:00Z_
_Verifier: Claude (gsd-verifier)_
