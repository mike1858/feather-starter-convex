---
phase: 999.5-excel-to-system-pipeline-backlog
plan: 2
subsystem: pipeline
tags: [llm, anthropic, excel, yaml-generation, entity-classification, topological-sort]

requires:
  - phase: 999.5-01
    provides: Excel parser, type inference engine, SheetMetadata/InferredEntity types
provides:
  - Entity classifier with FK relationship detection (xId, x_id, name match)
  - Topological sort for import ordering (Kahn's algorithm)
  - Convex analyzeExcel action with LLM + heuristic fallback
  - feather.yaml generator producing schema-valid YAML from InferredEntity
affects: [999.5-03-confirmation-wizard, 999.5-04-data-import-execution, 999.5-05-ui-routes]

tech-stack:
  added: ["@anthropic-ai/sdk", "xlsx"]
  patterns: ["LLM with heuristic fallback", "Convex 'use node' actions for npm packages", "topological sort via Kahn's algorithm"]

key-files:
  created:
    - templates/pipeline/excel/entity-classifier.ts
    - templates/pipeline/excel/entity-classifier.test.ts
    - templates/pipeline/excel/yaml-generator.ts
    - templates/pipeline/excel/yaml-generator.test.ts
    - convex/imports/actions.ts
    - convex/imports/actions.test.ts
    - convex/imports/mutations.ts
    - templates/pipeline/excel/parser.ts
    - templates/pipeline/excel/type-inference.ts
    - templates/pipeline/excel/index.ts
  modified:
    - convex/schema.ts
    - convex/_generated/api.d.ts
    - package.json

key-decisions:
  - "Used api (not internal) for action-to-mutation calls since imports mutations are public"
  - "Identity defaults to auto-increment in generated YAML (matching existing feather.yaml pattern)"
  - "StatusFlow includes linear transitions auto-generated from enum values"
  - "Relationships use belongs_to with column property (matching featureYamlSchema discriminated union)"
  - "Created Plan 01 prerequisite files (parser.ts, type-inference.ts) as blocking dependency resolution"

patterns-established:
  - "LLM fallback pattern: try Claude Sonnet -> catch -> use heuristic inference"
  - "YAML generation pattern: build JS object -> yaml.stringify with lineWidth 0"
  - "Relationship detection: three-pattern approach (Id suffix, name match, value overlap)"

requirements-completed: []

duration: 33min
completed: 2026-04-01
---

# Plan 999.5-02: LLM Schema Analysis Action and YAML Generation Summary

**Entity classifier with FK detection, Convex analyzeExcel action (LLM + heuristic fallback), and feather.yaml generator producing schema-valid YAML**

## Performance

- **Duration:** 33 min
- **Started:** 2026-04-01T03:40:41Z
- **Completed:** 2026-04-01T04:14:06Z
- **Tasks:** 4
- **Files modified:** 13

## Accomplishments
- Entity classifier detects FK relationships via xId suffix, x_id suffix, and field-name-matches-entity patterns with singular/plural handling
- Topological sort (Kahn's algorithm) computes import order with cycle detection
- Convex `analyzeExcel` action downloads file from storage, parses with SheetJS, tries Claude Sonnet analysis, falls back to heuristic inference
- YAML generator produces valid feather.yaml files that pass `validateFeatureYaml()` validation
- 27 tests across 3 test files covering all major code paths

## Task Commits

Each task was committed atomically:

1. **Task 1: Build entity classifier with relationship detection** - `b6f5e08` (feat)
2. **Task 2: Build Convex action for LLM schema analysis** - `326ad06` (feat)
3. **Task 3: Build feather.yaml generator from confirmed schema** - `0987f0b` (feat)
4. **Task 4: Run full verification for Plan 02** - `c53863c` (chore)

## Files Created/Modified
- `templates/pipeline/excel/entity-classifier.ts` - Relationship detection (3 patterns) and topological sort
- `templates/pipeline/excel/entity-classifier.test.ts` - 10 tests for relationship detection and import ordering
- `templates/pipeline/excel/yaml-generator.ts` - Converts InferredEntity to valid feather.yaml YAML content
- `templates/pipeline/excel/yaml-generator.test.ts` - 11 tests including validation against featureYamlSchema
- `convex/imports/actions.ts` - analyzeExcel action with LLM + heuristic fallback
- `convex/imports/actions.test.ts` - 6 tests for heuristic analysis and prompt building
- `convex/imports/mutations.ts` - CRUD mutations for imports table (Plan 01 prerequisite)
- `templates/pipeline/excel/parser.ts` - Excel workbook parser (Plan 01 prerequisite)
- `templates/pipeline/excel/type-inference.ts` - Heuristic type inference engine (Plan 01 prerequisite)
- `templates/pipeline/excel/index.ts` - Barrel exports for all pipeline/excel modules
- `convex/schema.ts` - Added imports, schemaMappings, importErrors tables
- `convex/_generated/api.d.ts` - Added imports/actions and imports/mutations type declarations
- `package.json` - Added @anthropic-ai/sdk and xlsx dependencies

## Decisions Made
- Used `api` (not `internal`) for action-to-mutation calls since imports mutations are public
- Identity defaults to `auto-increment` in generated YAML (matching existing feather.yaml pattern)
- StatusFlow includes auto-generated linear transitions from enum values (open -> in_progress -> closed)
- Relationships use `belongs_to` with `column` property to match featureYamlSchema discriminated union requirements
- Created Plan 01 prerequisite files in this plan since Plan 01 hadn't executed yet (parallel execution)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created Plan 01 prerequisite files (parser, type-inference, index)**
- **Found during:** Task 1 (entity classifier depends on Plan 01 types)
- **Issue:** Plan 01 hadn't executed, so parser.ts, type-inference.ts, and index.ts didn't exist
- **Fix:** Created all three files using the exact code from Plan 01's specification
- **Files modified:** templates/pipeline/excel/parser.ts, templates/pipeline/excel/type-inference.ts, templates/pipeline/excel/index.ts
- **Verification:** Entity classifier tests pass using these types
- **Committed in:** b6f5e08 (Task 1 commit)

**2. [Rule 3 - Blocking] Installed xlsx and @anthropic-ai/sdk dependencies**
- **Found during:** Task 2 (action imports xlsx via parser, uses Anthropic SDK)
- **Issue:** Neither package was installed (Plan 01 installs xlsx, Plan 02 installs @anthropic-ai/sdk)
- **Fix:** Ran npm install for both packages
- **Files modified:** package.json, package-lock.json
- **Verification:** Tests pass with imports resolved
- **Committed in:** 326ad06 (Task 2 commit)

**3. [Rule 3 - Blocking] Created convex/imports/mutations.ts and added schema tables**
- **Found during:** Task 2 (action calls saveAnalysis mutation) and Task 4 (typecheck)
- **Issue:** convex/imports/mutations.ts didn't exist (Plan 01 task), schema.ts didn't have imports/schemaMappings/importErrors tables
- **Fix:** Created mutations.ts with all CRUD operations; added 3 tables to schema.ts
- **Files modified:** convex/imports/mutations.ts, convex/schema.ts
- **Verification:** Typecheck passes for all new files
- **Committed in:** 326ad06 (mutations), c53863c (schema tables)

**4. [Rule 1 - Bug] Fixed YAML generator to match actual schema**
- **Found during:** Task 3 (reading featureYamlSchema and related schemas)
- **Issue:** Plan specified `identity: { displayField: ... }` but identitySchema requires `{ type: "auto-increment" | "uuid" | "expression" }`. Plan specified `statusFlow: { field, defaultStatus }` but statusFlowSchema requires `{ field, transitions }`. Plan specified `views: { default: { defaultView, enabledViews } }` but viewsSchema uses flat `{ defaultView, enabledViews }`.
- **Fix:** Corrected all three to match actual schema definitions; added linear transition generation for statusFlow
- **Files modified:** templates/pipeline/excel/yaml-generator.ts
- **Verification:** Generated YAML passes validateFeatureYaml() validation in tests
- **Committed in:** 0987f0b (Task 3 commit)

**5. [Rule 1 - Bug] Fixed singular/plural matching in Pattern 2 name match**
- **Found during:** Task 1 (entity classifier test failures)
- **Issue:** Pattern 2 only checked exact field-to-entity name match, so "department" didn't match "departments"
- **Fix:** Added singular/plural comparison (field + "s" === target, field === target without trailing "s")
- **Files modified:** templates/pipeline/excel/entity-classifier.ts
- **Verification:** All 10 entity classifier tests pass
- **Committed in:** b6f5e08 (Task 1 commit)

---

**Total deviations:** 5 auto-fixed (2 bugs, 3 blocking)
**Impact on plan:** All deviations were necessary for correctness. Blocking issues caused by Plan 01 not having executed yet in parallel workflow. Bug fixes corrected plan code that didn't match actual schema structures. No scope creep.

## Issues Encountered
- Pre-existing typecheck errors in TaskFilterBar.tsx, TasksPage.tsx, TeamPoolPage.tsx, ResendOTP.ts, ResendOTPPasswordReset.ts -- not from this plan's changes, left untouched per scope boundary rules

## User Setup Required
None - no external service configuration required. ANTHROPIC_API_KEY is optional (heuristic fallback works without it).

## Next Phase Readiness
- Entity classifier, YAML generator, and analyzeExcel action are ready for Plan 03 (Confirmation Wizard)
- Plan 03 can import from `templates/pipeline/excel/` barrel exports
- YAML generator output passes featureYamlSchema validation, ready for gen:feature pipeline

---
*Phase: 999.5-excel-to-system-pipeline-backlog*
*Completed: 2026-04-01*

## Self-Check: PASSED
- All 10 created files exist on disk
- All 4 task commits found in git history (b6f5e08, 326ad06, 0987f0b, c53863c)
