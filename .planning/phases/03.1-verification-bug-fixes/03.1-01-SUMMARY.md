---
phase: 03.1-verification-bug-fixes
plan: 01
subsystem: auth, tasks, infra
tags: [resend, otp, jwt, convex, tanstack-router, dev-tooling]

requires:
  - phase: 03-tasks
    provides: task mutations with assign handler
provides:
  - task unassign auto-flips visibility to shared (Team Pool)
  - Resend skip for local dev (OTP and password reset work without API key)
  - JWT auto-provisioning via predev script
  - auth provider availability query (password, otp, github)
  - dev mailbox route accessible without authentication
affects: [03.1-verification-bug-fixes]

tech-stack:
  added: [@convex-dev/auth CLI for JWT provisioning]
  patterns: [early-return guard for missing API keys, public query for provider availability]

key-files:
  created:
    - convex/auth/queries.ts
    - convex/auth/queries.test.ts
    - src/routes/_app/dev/mailbox.tsx
  modified:
    - convex/tasks/mutations.ts
    - convex/tasks/mutations.test.ts
    - convex/otp/ResendOTP.ts
    - convex/password/ResendOTPPasswordReset.ts
    - package.json
    - src/routeTree.gen.ts
    - convex/_generated/api.d.ts

key-decisions:
  - "availableProviders query is public (no auth guard) because login page needs it pre-authentication"
  - "Resend skip uses early return after dev mailbox store when provider.apiKey is falsy"
  - "Dev mailbox moved from _auth to _app layout to bypass auth guard while keeping sidebar nav"

patterns-established:
  - "Early-return guard pattern for optional external service credentials"
  - "Public availability queries for conditional UI rendering of auth providers"

requirements-completed: [BUG-UNASSIGN, DX-JWT-KEYS, DX-RESEND-SKIP, DX-MAILBOX-AUTH, DX-MAILBOX-LAYOUT, UX-HIDE-PROVIDERS]

duration: 7min
completed: 2026-03-25
---

# Phase 03.1 Plan 01: Backend Bug Fixes and Dev Infrastructure Summary

**Task unassign visibility auto-flip, Resend skip for keyless local dev, JWT auto-provisioning, auth provider availability query, and dev mailbox auth bypass**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-25T06:16:55Z
- **Completed:** 2026-03-25T06:23:47Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- Unassigning a task now auto-flips visibility to "shared" so it appears in Team Pool
- OTP and password reset flows complete without Resend API key (dev mailbox stores email, Resend send skipped)
- predev script auto-provisions JWT signing keys before starting Convex backend
- New availableProviders query reports which auth providers have credentials configured
- Dev mailbox route accessible without authentication (moved outside _auth layout)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix unassign visibility bug and Resend skip for local dev** (TDD)
   - `507a472` test(03.1-01): add failing test for unassign visibility auto-flip
   - `44717c6` feat(03.1-01): fix unassign visibility bug and Resend skip for local dev
2. **Task 2: JWT auto-provisioning, dev mailbox route fix, and auth provider query** - `f6ec27c` (feat)

## Files Created/Modified
- `convex/tasks/mutations.ts` - Added unassign auto-flip to shared visibility
- `convex/tasks/mutations.test.ts` - New test for unassign-to-shared, renamed existing test
- `convex/otp/ResendOTP.ts` - Skip Resend API call when provider.apiKey is falsy
- `convex/password/ResendOTPPasswordReset.ts` - Same Resend skip pattern
- `package.json` - predev script with JWT key auto-provisioning
- `convex/auth/queries.ts` - New availableProviders public query
- `convex/auth/queries.test.ts` - Test for provider availability
- `src/routes/_app/dev/mailbox.tsx` - Dev mailbox route outside auth guard
- `src/routeTree.gen.ts` - Updated route tree for new mailbox location
- `convex/_generated/api.d.ts` - Added auth/queries module type

## Decisions Made
- availableProviders query is public (no auth guard) because the login page needs it before user is authenticated; it only exposes boolean availability, not actual credentials
- Resend skip uses early return after dev mailbox store when provider.apiKey is falsy -- simplest pattern, no conditional wrapping
- Dev mailbox moved from `_auth` to `_app` layout to bypass auth guard while keeping sidebar nav context

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Worktree was behind main by 8 commits (Phase 3 work) -- fast-forward merged to get task mutations code
- Vitest v4 uses `--bail=1` instead of `-x` flag for early termination

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Backend bugs fixed, ready for Plan 02 (frontend fixes)
- availableProviders query ready for frontend consumption in login page conditional rendering
- Dev mailbox now accessible for testing OTP/password reset flows without auth

## Self-Check: PASSED

All 10 files verified (7 modified, 3 created). Old route file confirmed deleted. All 3 commits found in git log.

---
*Phase: 03.1-verification-bug-fixes*
*Completed: 2026-03-25*
