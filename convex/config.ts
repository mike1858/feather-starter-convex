/**
 * Backend branding config — single source of truth for Convex functions.
 *
 * The setup script (`npm run setup`) updates this file.
 * Frontend branding lives in `site.config.ts` at the project root.
 */
export const APP_NAME = "Feather Starter";
